{
  AbstractPlanNode pn=null;
  pn=compile("select a from t where a < ?;",1,true);
  assertTrue(pn != null);
  pn=pn.getChild(0);
  if (pn != null) {
    System.out.println(pn.toJSONString());
  }
  assertTrue(pn instanceof IndexScanPlanNode);
}
