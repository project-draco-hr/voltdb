{
  StringBuilder sb=new StringBuilder();
  boolean first=false;
  for (  Long id : ids) {
    if (!first) {
      first=true;
    }
 else {
      sb.append(", ");
    }
    sb.append(id.intValue()).append(':').append((int)(id.longValue() >> 32));
  }
  return sb.toString();
}
