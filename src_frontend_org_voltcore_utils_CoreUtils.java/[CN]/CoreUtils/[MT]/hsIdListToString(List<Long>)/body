{
  if (hsIds == null || hsIds.size() == 0) {
    return "<none>";
  }
  String result="";
  boolean first=true;
  for (  long hsId : hsIds) {
    result+=hsIdToString(hsId);
    if (!first)     result+=",";
  }
  return result;
}
