{
  if (hsIds == null || hsIds.size() == 0) {
    return "<none>";
  }
  String result="";
  boolean first=true;
  for (  long hsId : hsIds) {
    if (!first)     result+=", ";
    first=false;
    result+=hsIdToString(hsId);
  }
  return result;
}
