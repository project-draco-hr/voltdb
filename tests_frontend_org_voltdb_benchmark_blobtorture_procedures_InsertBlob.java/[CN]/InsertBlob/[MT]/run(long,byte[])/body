{
  voltQueueSQL(insertBlob,blobId,blob);
  System.err.println("Inserted " + voltExecuteSQL()[0].asScalarLong() + " blobs");
  voltQueueSQL(selectBlob,blobId);
  final VoltTable results[]=voltExecuteSQL(true);
  results[0].advanceRow();
  return results;
}
