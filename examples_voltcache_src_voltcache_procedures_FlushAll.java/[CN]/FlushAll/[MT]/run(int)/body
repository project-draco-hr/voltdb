{
  final int now=Shared.now(this);
  if (expires <= 0)   voltQueueSQL(delete);
 else {
    voltQueueSQL(cleanup,now);
    voltQueueSQL(update,Shared.expires(this,expires));
  }
  voltExecuteSQL(true);
  return VoltCacheResult.OK;
}
