{
  if (expires <= 0) {
    voltQueueSQL(delete);
  }
 else {
    voltQueueSQL(cleanup,now());
    voltQueueSQL(update,expirationTimestamp(expires));
  }
  voltExecuteSQL(true);
  return Result.OK;
}
