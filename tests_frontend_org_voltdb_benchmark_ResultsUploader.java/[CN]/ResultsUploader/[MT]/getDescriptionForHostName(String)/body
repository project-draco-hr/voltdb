{
  if (hostname.contains("amazonaws.com"))   return "amazonws";
  if (hostname.contains("-bl"))   return "desktop";
  if (hostname.contains("-gr"))   return "desktop";
  if (hostname.contains("localhost"))   return "desktop";
 else   return "unknown";
}
