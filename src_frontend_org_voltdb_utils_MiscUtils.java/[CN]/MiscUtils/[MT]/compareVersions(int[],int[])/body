{
  if (left == null || right == null) {
    throw new IllegalArgumentException("Invalid versions");
  }
  int i=0;
  for (  int part : right) {
    if (left.length == i) {
      return -1;
    }
    if (left[i] > part) {
      return 1;
    }
 else     if (left[i] < part) {
      return -1;
    }
    i++;
  }
  if (left.length > i) {
    return 1;
  }
  return 0;
}
