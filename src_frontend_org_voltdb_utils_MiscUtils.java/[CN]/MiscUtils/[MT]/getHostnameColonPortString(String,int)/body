{
  server=server.trim();
  String[] parts=server.trim().split(":");
  if (parts.length == 1) {
    if (server.endsWith(":"))     return server + String.valueOf(0);
    return server + ":" + String.valueOf(defaultPort);
  }
 else {
    assert(parts.length == 2);
    return server;
  }
}
