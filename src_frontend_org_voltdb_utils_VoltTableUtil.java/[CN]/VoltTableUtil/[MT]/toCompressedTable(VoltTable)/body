{
  VoltTable retval=new VoltTable(m_compressedTableColumns);
  try {
    retval.addRow(table.getCompressedBytes());
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
  return retval;
}
