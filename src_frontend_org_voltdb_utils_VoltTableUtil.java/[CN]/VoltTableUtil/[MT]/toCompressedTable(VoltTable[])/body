{
  VoltTable retval=new VoltTable(m_compressedTableColumns);
  try {
    for (    VoltTable vt : tables) {
      retval.addRow(vt.getCompressedBytes());
    }
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
  return retval;
}
