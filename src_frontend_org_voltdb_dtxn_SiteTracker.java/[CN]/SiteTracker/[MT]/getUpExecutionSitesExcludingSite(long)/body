{
  Set<Long> tmplist=m_mailboxTracker.getAllSites();
  int size=tmplist.size();
  if (tmplist.contains(excludedSite))   size--;
  long[] retval=new long[size];
  int i=0;
  for (  long id : tmplist) {
    if (id != excludedSite)     retval[i++]=id;
  }
  return retval;
}
