{
  List<Item> response=new LinkedList<Item>();
  Iterator<Item> it=m_log.iterator();
  while (it.hasNext()) {
    Item i=it.next();
    if (!forMPI || i.isMP()) {
      response.add(i);
    }
  }
  return response;
}
