{
  if (spHandle == Long.MIN_VALUE) {
    return;
  }
  m_truncationPoint=spHandle;
  while (!m_log.isEmpty()) {
    if (m_log.peek().m_handle <= m_truncationPoint) {
      m_log.poll();
      continue;
    }
    break;
  }
}
