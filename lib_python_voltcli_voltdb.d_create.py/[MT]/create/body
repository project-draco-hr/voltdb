@VOLT.Command(bundles=VOLT.ServerBundle('create'), options=(VOLT.BooleanOption('-r', '--replica', 'replica', 'start replica cluster', default=False),), description='Start the VoltDB server and create the database.')
def create(runner):
    runner.go()
