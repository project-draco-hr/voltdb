@VOLT.Command(bundles=VOLT.ServerBundle('create'), options=(VOLT.BooleanOption('-r', '--replica', 'replica', 'start replica cluster', default=False),), description='Start a new, empty database.')
def create(runner):
    runner.go()
