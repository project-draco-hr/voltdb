def Arity(n):
    'Returns the English name of the given arity.'
    if (n < 0):
        return None
    elif (n <= 3):
        return ['nullary', 'unary', 'binary', 'ternary'][n]
    else:
        return ('%s-ary' % n)
