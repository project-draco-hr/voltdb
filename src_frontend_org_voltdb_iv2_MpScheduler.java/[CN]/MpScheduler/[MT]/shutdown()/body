{
  SiteTasker nullTask=new SiteTasker(){
    @Override public void run(    SiteProcedureConnection siteConnection){
    }
    @Override public void runForRejoin(    SiteProcedureConnection siteConnection){
    }
    @Override public int priority(){
      return 0;
    }
  }
;
  m_pendingTasks.repair(nullTask);
}
