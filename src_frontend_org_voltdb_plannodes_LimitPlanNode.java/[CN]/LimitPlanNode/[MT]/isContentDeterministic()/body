{
  boolean precondition=m_children.get(0).isOrderDeterministic();
  if (precondition) {
    return true;
  }
  m_nondeterminismDetail="a limit on unordered content may return different rows: " + m_children.get(0).nondeterminismDetail();
  return false;
}
