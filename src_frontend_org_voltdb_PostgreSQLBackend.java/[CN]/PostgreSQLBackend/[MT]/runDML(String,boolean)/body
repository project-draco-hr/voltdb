{
  String modifiedDml=(transformDml ? transformDML(dml) : dml);
  debugPrintTransformSql(dml,modifiedDml,transformDml && dml != null && !dml.equals(modifiedDml));
  return super.runDML(modifiedDml);
}
