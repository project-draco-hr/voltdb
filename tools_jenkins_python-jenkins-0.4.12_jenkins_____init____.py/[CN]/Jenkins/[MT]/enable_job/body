def enable_job(self, name):
    'Enable Jenkins job.\n\n        :param name: Name of Jenkins job, ``str``\n        '
    (folder_url, short_name) = self._get_job_folder(name)
    self.jenkins_open(Request(self._build_url(ENABLE_JOB, locals()), ''))
