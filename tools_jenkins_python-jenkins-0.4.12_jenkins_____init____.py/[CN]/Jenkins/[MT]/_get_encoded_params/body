def _get_encoded_params(self, params):
    for (k, v) in params.items():
        if (k in ['name', 'msg', 'short_name', 'from_short_name', 'to_short_name', 'folder_url', 'from_folder_url', 'to_folder_url']):
            params[k] = quote(v)
    return params
