{
  m_username="default";
  m_password="password";
  VoltFile.recursivelyDelete(new File("/tmp/" + System.getProperty("user.name")));
  File f=new File("/tmp/" + System.getProperty("user.name"));
  f.mkdirs();
  super.setUp();
  m_verifier=new ExportToFileVerifier(getServerConfig().getPathInSubroots(new File("/tmp/" + System.getProperty("user.name"))),"zorag");
  m_verifier.loadGenerations();
}
