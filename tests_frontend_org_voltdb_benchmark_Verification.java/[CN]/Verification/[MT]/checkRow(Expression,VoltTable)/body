{
  Object result=exp.evaluate(row);
  if (!(result instanceof Boolean))   return false;
  if (!((Boolean)result))   System.err.println("Failed check on: " + exp.toString(row));
  return (Boolean)result;
}
