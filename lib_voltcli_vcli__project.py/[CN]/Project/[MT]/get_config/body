def get_config(self, tag, required=False):
    text = self.root.findtext(('info/%s' % tag))
    if (required and (text is None)):
        vcli_util.abort(('"%s" is not configured in "%s".' % (tag, self.project_path)), 'Use the "config" command to set (or manually edit).')
    return text
