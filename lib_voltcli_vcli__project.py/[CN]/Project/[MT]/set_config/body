def set_config(self, tag, value):
    info = self.root.find('info')
    e = info.find(tag)
    if (e is not None):
        e.text = value
    else:
        child = ElementTree.Element(tag)
        child.text = value
        info.append(child)
