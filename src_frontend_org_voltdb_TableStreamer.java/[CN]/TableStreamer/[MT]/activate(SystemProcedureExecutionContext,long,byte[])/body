{
  if (!context.activateTableStream(m_tableId,m_type,undoToken,predicates)) {
    VoltDB.crashLocalVoltDB("Attempted to activate copy on write mode for table " + m_tableId + " and failed",false,null);
  }
}
