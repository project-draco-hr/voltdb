{
  if (!(peer_ instanceof CreateResponse)) {
    return false;
  }
  if (peer_ == this) {
    return true;
  }
  CreateResponse peer=(CreateResponse)peer_;
  boolean ret=false;
  ret=path.equals(peer.path);
  if (!ret)   return ret;
  return ret;
}
