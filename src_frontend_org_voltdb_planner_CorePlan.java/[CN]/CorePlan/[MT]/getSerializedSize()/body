{
  int size=4 + aggregatorFragment.length + 20;
  if (collectorFragment != null) {
    size+=4 + collectorFragment.length + 20;
  }
 else {
    size+=4;
  }
  size+=3;
  size+=4;
  size+=2;
  size+=parameterTypes.length;
  return size;
}
