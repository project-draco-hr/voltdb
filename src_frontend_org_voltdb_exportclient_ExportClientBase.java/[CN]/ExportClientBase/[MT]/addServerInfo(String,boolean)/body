{
  int defaultPort=adminIfNoPort ? VoltDB.DEFAULT_ADMIN_PORT : VoltDB.DEFAULT_PORT;
  InetSocketAddress addr=new InetSocketAddress(MiscUtils.getHostnameFromHostnameColonPort(server),MiscUtils.getPortFromHostnameColonPort(server,defaultPort));
  m_servers.add(addr);
}
