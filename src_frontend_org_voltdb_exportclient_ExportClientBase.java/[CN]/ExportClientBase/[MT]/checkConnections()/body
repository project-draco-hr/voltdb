{
  boolean retval=true;
  for (  ExportConnection connection : m_exportConnections.values()) {
    if (!connection.isConnected()) {
      m_logger.error("Lost connection: " + connection.name + ", Closing...");
      retval=false;
    }
  }
  return retval;
}
