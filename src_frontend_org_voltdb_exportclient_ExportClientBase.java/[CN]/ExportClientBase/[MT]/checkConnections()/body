{
  boolean retval=true;
  for (  String el_connection : m_exportConnections.keySet()) {
    if (!m_exportConnections.get(el_connection).isConnected()) {
      m_logger.error("Lost connection: " + el_connection + ", Closing...");
      retval=false;
    }
  }
  return retval;
}
