{
  pns=compileToFragments(sql);
  for (  AbstractPlanNode apn : pns) {
  }
  checkMVFixWithWhere(aggFilter == null ? null : new String[]{aggFilter},scanFilter == null ? null : new String[]{scanFilter});
}
