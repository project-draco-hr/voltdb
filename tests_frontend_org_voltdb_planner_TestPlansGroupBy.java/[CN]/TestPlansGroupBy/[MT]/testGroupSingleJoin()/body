{
  AbstractPlanNode pn=null;
  pn=compile("select D1.D1_NAME, sum(V.SUM_V1), sum(V.SUM_V2), sum(V.SUM_V3) " + "from D1, V where D1.D1_PKEY = V.V_D1_PKEY " + "group by D1.D1_NAME",0);
  if (pn != null)   System.out.println(pn.toJSONString());
}
