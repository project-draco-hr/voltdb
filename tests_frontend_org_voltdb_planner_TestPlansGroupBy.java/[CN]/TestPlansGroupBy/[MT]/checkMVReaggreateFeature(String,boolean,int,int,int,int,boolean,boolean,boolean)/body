{
  pns=compileToFragments(sql);
  if (mergeReceive) {
    checkMVReaggregateFeatureMergeReceive(needFix,numGroupbyOfReaggNode,numAggsOfReaggNode,aggPushdown,aggInline);
  }
 else {
    checkMVReaggregateFeature(needFix,numGroupbyOfTopAggNode,numAggsOfTopAggNode,numGroupbyOfReaggNode,numAggsOfReaggNode,aggPushdown,aggInline);
  }
}
