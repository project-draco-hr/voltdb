{
  m_hostCount=1;
  buildCatalog(m_hostCount,8,0,newVoltRoot(null));
  MockInitiator initiator=new MockInitiator(null);
  RestoreAgent restoreAgent=new RestoreAgent(context,initiator,getClient(0),this,0,START_ACTION.START);
  restoreAgent.restore();
  while (!m_done) {
    try {
      Thread.sleep(200);
    }
 catch (    InterruptedException e) {
    }
  }
}
