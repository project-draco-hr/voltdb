def __init__(self, fser):
    self.fser = fser
    self.version = (-1)
    self.clientHandle = (-1)
    self.status = (-1)
    self.statusString = ''
    self.appStatus = (-1)
    self.appStatusString = ''
    self.roundtripTime = (-1)
    self.exception = None
    self.tables = None
    if (fser != None):
        self.deserialize(fser)
