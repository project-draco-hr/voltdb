{
  String path=String.format("%s%s%s%d%s%s",m_sampleDir.getPath(),File.separator,SAMPLE_FILE_PREFIX,txnId,initiatorId != NON_REPLAY_SAMPLE ? REPLAY_TOKEN + String.valueOf(initiatorId) : "",SAMPLE_FILE_EXTENSION);
  return new File(path);
}
