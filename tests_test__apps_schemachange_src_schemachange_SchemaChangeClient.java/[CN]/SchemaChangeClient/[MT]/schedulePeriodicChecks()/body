{
  timer=new Timer(true);
  TimerTask statsPrinting=new TimerTask(){
    @Override public void run(){
      periodicChecks();
    }
  }
;
  timer.scheduleAtFixedRate(statsPrinting,config.checkInterval * 1000,config.checkInterval * 1000);
}
