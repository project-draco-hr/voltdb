{
  StreamSnapshotRequestConfig.Stream stream=new StreamSnapshotRequestConfig.Stream(sourceToDests,null,null);
  StreamSnapshotRequestConfig config=new StreamSnapshotRequestConfig(null,Arrays.asList(stream));
  return makeSnapshotRequest(config);
}
