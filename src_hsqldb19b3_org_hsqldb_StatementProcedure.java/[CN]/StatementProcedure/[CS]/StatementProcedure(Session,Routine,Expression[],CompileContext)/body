{
  super(StatementTypes.CALL,StatementTypes.X_SQL_DATA,session.currentSchema);
  this.procedure=procedure;
  this.arguments=arguments;
  setDatabseObjects(compileContext);
  checkAccessRights(session);
}
