{
  hostLog.fatal("Usage: org.voltdb.VoltDB start|recover|create leader|rejoinhost <hostname> " + "deployment <deployment.xml> license <license.xml> [catalog <catalog.jar>]");
  hostLog.fatal("The _Getting Started With VoltDB_ book explains how " + "to run VoltDB from the command line.");
}
