{
  try {
    if (m_importerFactory != null) {
      m_importerTypeMgr.stop();
    }
    if (m_bundle != null) {
      m_bundle.stop();
    }
  }
 catch (  Exception ex) {
    m_logger.error("Failed to stop the import bundles.",ex);
  }
}
