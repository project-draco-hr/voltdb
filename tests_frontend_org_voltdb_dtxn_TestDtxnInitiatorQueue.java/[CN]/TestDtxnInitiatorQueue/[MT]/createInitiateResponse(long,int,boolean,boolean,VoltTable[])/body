{
  InitiateTask task=new InitiateTask(INITIATOR_SITE_ID,coordId,txnId,readOnly,isSinglePart,new StoredProcedureInvocation(),Long.MAX_VALUE);
  InitiateResponse response=new InitiateResponse(task);
  response.setResults(new ClientResponseImpl((byte)0,results,""),task);
  return response;
}
