{
  Timer timer=new Timer();
  TimerTask statsPrinting=new TimerTask(){
    @Override public void run(){
      printStatistics(periodicStatsContext,startTime);
    }
  }
;
  timer.scheduleAtFixedRate(statsPrinting,2 * 1000,2 * 1000);
  return timer;
}
