{
  inCount.set(0);
  outCount.set(0);
  errorInfo.clear();
  csvReader.close();
  csvClient.close();
  out_invaliderowfile.flush();
  out_logfile.flush();
  out_reportfile.flush();
  out_invaliderowfile.close();
  out_logfile.close();
  out_reportfile.close();
}
