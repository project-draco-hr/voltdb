{
  if (procedureClassName == null)   return null;
  if (procedureClassName.startsWith("org.voltdb."))   return Class.forName(procedureClassName);
  return m_jarfile.getLoader().loadClass(procedureClassName);
}
