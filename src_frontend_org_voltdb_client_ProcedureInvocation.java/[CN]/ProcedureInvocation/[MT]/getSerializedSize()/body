{
  try {
    m_procNameBytes=m_procName.getBytes("UTF-8");
  }
 catch (  Exception e) {
  }
  int size=1 + (m_type == ProcedureInvocationType.REPLICATED ? 8 : 0) + m_procNameBytes.length+ 4+ 8+ m_parameters.getSerializedSize();
  return size;
}
