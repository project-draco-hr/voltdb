def _flatten(*items):
    for item in items:
        try:
            test_string = (item + '')
            yield item
        except TypeError:
            try:
                test_iter = iter(item)
                for subitem in item:
                    for subsubitem in _flatten(subitem):
                        yield subsubitem
            except TypeError:
                yield item
