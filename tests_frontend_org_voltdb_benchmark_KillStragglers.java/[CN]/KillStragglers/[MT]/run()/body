{
  if (m_remotePath != null) {
    ShellTools.cmdToStdOut(SSHTools.convert(m_username,m_hostname,m_remotePath,"/bin/bash killstragglers.sh"));
  }
 else {
    String[] cmd={"/bin/bash","test/killstragglers.sh"};
    ShellTools.cmdToStdOut(cmd);
  }
}
