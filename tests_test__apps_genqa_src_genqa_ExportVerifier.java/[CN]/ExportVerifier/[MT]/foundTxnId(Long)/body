{
  boolean found=false;
  for (int i=0; i < m_partitions; i++) {
    if (m_rowTxnIds.get(i).contains(txnId)) {
      m_rowTxnIds.get(i).remove(txnId);
      found=true;
      break;
    }
  }
  return found;
}
