{
  InFlightTxnState retval=new InFlightTxnState(txnId,coordIds[0],null,new long[]{},readOnly,true,new StoredProcedureInvocation(),new Object(),0,0,0,"",false,allowMismatched);
  for (int i=1; i < coordIds.length; i++) {
    retval.addCoordinator(coordIds[i]);
  }
  return retval;
}
