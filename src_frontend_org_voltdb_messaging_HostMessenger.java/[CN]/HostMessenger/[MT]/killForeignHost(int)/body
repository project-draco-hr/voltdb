{
  for (int i=0; i < m_foreignHosts.length; i++) {
    ForeignHost host=m_foreignHosts[i];
    if ((host != null) && (host.isUp())) {
      if (i == hostId) {
        host.m_isUp=false;
        return;
      }
    }
  }
}
