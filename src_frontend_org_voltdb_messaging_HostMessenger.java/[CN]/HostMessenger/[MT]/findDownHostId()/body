{
  for (int hostId=0; hostId < m_foreignHosts.length; hostId++) {
    if (hostId == m_localHostId) {
      continue;
    }
    ForeignHost fh=m_foreignHosts[hostId];
    if (fh == null || (fh.isUp() == false))     return hostId;
  }
  return -1;
}
