{
  ByteBuffer msg=createMsg("@Statistics","DR",0);
  ClientResponseImpl resp=m_ci.handleRead(msg,m_handler,m_cxn);
  assertNull(resp);
  verify(m_statsAgent).collectStats(any(Connection.class),anyInt(),eq(OpsSelector.STATISTICS),any(ParameterSet.class));
}
