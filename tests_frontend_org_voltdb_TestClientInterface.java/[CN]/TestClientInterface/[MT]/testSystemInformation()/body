{
  ByteBuffer msg=createMsg("@SystemInformation");
  ClientResponseImpl resp=m_ci.handleRead(msg,m_handler,m_cxn);
  assertNull(resp);
  verify(m_statsAgent).collectStats(any(Connection.class),anyInt(),eq("SYSTEMINFORMATION"),any(ParameterSet.class));
}
