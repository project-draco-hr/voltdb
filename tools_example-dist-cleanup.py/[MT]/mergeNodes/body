def mergeNodes(base, build, getName):
    retval = []
    for x in base:
        found = False
        for y in build:
            if (getName(x) == getName(y)):
                found = True
        if (not found):
            retval.append(x)
    for x in build:
        retval.append(x)
    return retval
