def copyEnterpriseFilesToReleaseDir(releaseDir, version, operatingsys):
    get(('%s/pro/obj/pro/voltdb-ent-%s.tar.gz' % (builddir, version)), ('%s/%s-voltdb-ent-%s.tar.gz' % (releaseDir, operatingsys, version)))
