def copyEnterpriseZipToReleaseDir(releaseDir, version, operatingsys):
    get(('%s/pro/obj/pro/voltdb-ent-%s.zip' % (builddir, version)), ('%s/%s-voltdb-ent-%s.zip' % (releaseDir, operatingsys, version)))
