def kill_voltdb():
    killed = subprocess.call("ps -fwwC java | grep org.voltdb.VoltDB | tr -s ' ' | cut -d ' ' -f 2 | xargs /bin/kill -KILL", shell=True)
    if (killed != 0):
        print  >> sys.stderr, 'Failed to kill the VoltDB server process'
