{
  if (name == null) {
    if (alias == null) {
      return "<none>";
    }
    return "(" + alias + ")";
  }
  if (alias == null || name.equals(alias)) {
    return name;
  }
  return name + "(" + alias+ ")";
}
