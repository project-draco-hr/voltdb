{
  super.validate();
  if ((m_right != null) || (m_left != null))   throw new Exception("ERROR: A value expression has child expressions for '" + this + "'");
  if (m_columnIndex < 0) {
    throw new Exception("ERROR: Invalid column index '" + m_columnIndex + "' for '"+ this+ "'");
  }
}
