{
  if (name != null) {
    if (alias != null && !name.equals(alias)) {
      return String.format("%s(%s)",name,alias);
    }
 else {
      return name;
    }
  }
 else   if (alias != null) {
    return String.format("(%s)",alias);
  }
 else {
    return "<none>";
  }
}
