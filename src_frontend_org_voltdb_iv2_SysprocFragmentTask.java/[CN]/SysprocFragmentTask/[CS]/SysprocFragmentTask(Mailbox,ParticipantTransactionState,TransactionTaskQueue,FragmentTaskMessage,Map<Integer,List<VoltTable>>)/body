{
  super(txn,queue);
  m_initiator=mailbox;
  m_task=message;
  m_inputDeps=inputDeps;
  if (m_inputDeps == null) {
    m_inputDeps=new HashMap<Integer,List<VoltTable>>();
  }
  assert(m_task.isSysProcTask());
}
