{
  if ((m_params.length > 0) && (m_serializedSize == 2)) {
    throw new IllegalStateException("ParameterSet.getSerializedSize() called without a calculated serialized size.");
  }
  return m_serializedSize;
}
