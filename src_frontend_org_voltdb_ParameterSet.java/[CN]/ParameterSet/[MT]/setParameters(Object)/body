{
  for (  Object param : params)   if (param instanceof String)   if (((String)param).length() > VoltType.MAX_VALUE_LENGTH)   throw new VoltOverflowException("Parameter set value larger than allowed max " + VoltType.MAX_VALUE_LENGTH_STR);
  this.m_params=params;
}
