{
  VoltTable vt;
  for (int i=0; i < 1000; i++) {
    int f_d1=i % 10;
    int f_d2=i % 50;
    int f_d3=i % 100;
    vt=client.callProcedure("InsertF",pkey++,f_d1,f_d2,f_d3,2,(i * 10),(i % 2))[0];
    assertTrue(vt.getRowCount() == 1);
  }
  return pkey;
}
