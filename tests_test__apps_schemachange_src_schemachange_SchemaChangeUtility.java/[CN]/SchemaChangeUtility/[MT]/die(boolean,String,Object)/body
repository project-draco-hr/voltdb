{
  if (format != null) {
    String message=String.format(format,params);
    if (happy) {
      log.info(message);
    }
 else {
      log.error(message);
    }
  }
  System.exit(happy ? 0 : -1);
}
