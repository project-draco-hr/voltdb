{
  if (conditionNode.children.size() == 0)   return;
  VoltXMLElement exprNode=conditionNode.children.get(0);
  assert(where == null);
  where=parseExpressionTree(exprNode);
  assert(where != null);
  ExpressionUtil.finalizeValueTypes(where);
}
