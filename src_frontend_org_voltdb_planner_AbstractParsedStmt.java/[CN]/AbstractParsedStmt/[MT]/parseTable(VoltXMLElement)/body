{
  String tableName=tableNode.attributes.get("table");
  Table table=getTableFromDB(tableName);
  assert(table != null);
  JoinInfo joinInfo=parseTableConditions(tableNode);
  tableList.add(table);
  tableJoinList.put(table,joinInfo);
}
