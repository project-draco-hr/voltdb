{
  List<String> columnNameList=new ArrayList<String>();
  Table t2=(Table)table;
  for (int j=0; j < colIndex.length; ++j) {
    columnNameList.add(t2.getColumn(colIndex[j]).getName().statementName);
  }
  return columnNameList;
}
