{
  String tmp;
  String padding="                                                               ";
  tmp=s + padding;
  if (tmp.length() > l) {
    tmp=tmp.substring(0,l);
  }
  if (tmp.length() < l) {
    tmp=tmp + padding.substring(tmp.length());
  }
  return tmp;
}
