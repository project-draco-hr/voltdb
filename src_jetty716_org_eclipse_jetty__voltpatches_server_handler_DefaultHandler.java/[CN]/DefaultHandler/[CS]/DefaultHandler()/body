{
  try {
    URL fav=this.getClass().getClassLoader().getResource("org/eclipse/jetty_voltpatches/favicon.ico");
    if (fav != null)     _favicon=IO.readBytes(fav.openStream());
  }
 catch (  Exception e) {
    Log.warn(e);
  }
}
