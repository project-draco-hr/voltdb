{
  long sleep=500;
  do {
    if (m_pendingCallbacks.isEmpty()) {
      break;
    }
    LockSupport.parkNanos(TimeUnit.MICROSECONDS.toNanos(sleep));
    if (sleep < 5000) {
      sleep+=500;
    }
  }
 while (true);
}
