{
  assert(exp != null);
  return (exp.getExpressionType() != ExpressionType.OPERATOR_NOT && exp.getExpressionType() != ExpressionType.COMPARE_IN && exp.getExpressionType() != ExpressionType.OPERATOR_IS_NULL && !(exp instanceof AggregateExpression));
}
