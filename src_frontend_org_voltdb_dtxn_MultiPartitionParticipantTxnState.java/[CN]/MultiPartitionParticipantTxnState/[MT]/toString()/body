{
  return "MultiPartitionParticipantTxnState initiator: " + initiatorHSId + " coordinator: "+ m_isCoordinator+ " in-progress: "+ m_hasStartedWork+ " txnId: "+ TransactionIdManager.toString(txnId);
}
