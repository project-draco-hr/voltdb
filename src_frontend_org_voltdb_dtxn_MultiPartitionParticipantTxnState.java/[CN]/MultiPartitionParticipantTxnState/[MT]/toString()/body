{
  return "MultiPartitionParticipantTxnState initiator: " + initiatorSiteId + " coordinator: "+ m_isCoordinator+ " in-progress: "+ m_hasStartedWork+ " txnId: "+ TransactionIdManager.toString(txnId);
}
