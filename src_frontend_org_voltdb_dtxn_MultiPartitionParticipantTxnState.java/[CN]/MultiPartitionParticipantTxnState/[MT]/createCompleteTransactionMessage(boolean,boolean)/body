{
  CompleteTransactionMessage ft=new CompleteTransactionMessage(initiatorHSId,coordinatorSiteId,txnId,true,getHash(),rollback,requiresAck,false,false);
  hostLog.info("Created CompleteTransactionMessage with hash: " + String.valueOf(getHash()));
  return ft;
}
