{
  if (_inNIOBuffer != null && _inNIOBuffer.length() == 0) {
synchronized (this) {
      if (_inNIOBuffer != null && _inNIOBuffer.length() == 0) {
        _buffers.returnBuffer(_inNIOBuffer);
        _inNIOBuffer=null;
      }
    }
  }
}
