{
  if (cBuffer == null) {
    getNextBuffer(true);
    if (cBuffer == null) {
      assert m_eof;
      return -1;
    }
  }
  int retval=cBuffer.get();
  if (!cBuffer.hasRemaining()) {
    cBuffer=null;
    cContainer.discard();
    cContainer=null;
  }
  m_available--;
  return retval;
}
