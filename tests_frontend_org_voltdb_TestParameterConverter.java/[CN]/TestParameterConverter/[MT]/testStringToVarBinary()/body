{
  String t="1E3A";
  Object r=ParameterConverter.tryToMakeCompatible(true,false,byte[].class,null,t);
  assertTrue("expect varbinary",r.getClass() == byte[].class);
  assertEquals(t,Encoder.hexEncode((byte[])r));
}
