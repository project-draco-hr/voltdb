{
  Object r=ParameterConverter.tryToMakeCompatible(true,false,int.class,null,new Integer(1));
  assertTrue("expect integer",r.getClass() == Integer.class);
  assertEquals(1,((Integer)r).intValue());
}
