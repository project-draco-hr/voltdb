{
  Object r=ParameterConverter.tryToMakeCompatible(false,false,BigDecimal.class,null,"111.1");
  assertTrue("exepct decimal",r.getClass() == BigDecimal.class);
  assertEquals("111.1",((BigDecimal)r).toString());
}
