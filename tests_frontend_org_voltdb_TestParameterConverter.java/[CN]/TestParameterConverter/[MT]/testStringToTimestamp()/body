{
  TimestampType t=new TimestampType();
  Object r=ParameterConverter.tryToMakeCompatible(true,false,TimestampType.class,null,t);
  assertTrue("expect timestamp",r.getClass() == TimestampType.class);
  assertEquals(t,r);
}
