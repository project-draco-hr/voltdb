{
  Object r=ParameterConverter.tryToMakeCompatible(true,false,int.class,null,null);
  assertTrue("expect integer",r.getClass() == String.class);
  assertEquals(VoltType.NULL_STRING_OR_VARBINARY,r);
}
