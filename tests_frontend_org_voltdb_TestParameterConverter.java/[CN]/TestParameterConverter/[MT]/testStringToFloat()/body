{
  Object r=ParameterConverter.tryToMakeCompatible(true,false,float.class,null,"34.56");
  assertTrue("expect float",r.getClass() == Float.class);
  assertEquals(new Float(34.56),((Float)r).floatValue());
}
