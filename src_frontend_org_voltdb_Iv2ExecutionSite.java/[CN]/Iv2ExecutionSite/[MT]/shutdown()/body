{
  try {
    if (m_hsql != null) {
      m_hsql.shutdown();
    }
    if (m_ee != null) {
      m_ee.release();
    }
  }
 catch (  InterruptedException e) {
    hostLog.warn("Interrupted shutdown execution site.",e);
  }
}
