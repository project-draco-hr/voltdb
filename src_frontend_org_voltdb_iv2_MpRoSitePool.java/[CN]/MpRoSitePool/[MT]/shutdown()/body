{
  for (  MpRoSiteContext site : m_idleSites) {
    site.shutdown();
    site.joinThread();
  }
  for (  MpRoSiteContext site : m_busySites.values()) {
    site.shutdown();
    site.joinThread();
  }
}
