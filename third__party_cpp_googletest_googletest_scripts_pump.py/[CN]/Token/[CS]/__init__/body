def __init__(self, start=None, end=None, value=None, token_type=None):
    if (start is None):
        self.start = Eof()
    else:
        self.start = start
    if (end is None):
        self.end = Eof()
    else:
        self.end = end
    self.value = value
    self.token_type = token_type
