{
  quiesce(client);
  while (true) {
    try {
      tester.work();
    }
 catch (    IOException e) {
      System.out.println(e.toString());
      continue;
    }
    break;
  }
  assertTrue(tester.allRowsVerified());
  assertTrue(tester.verifyExportOffsets());
}
