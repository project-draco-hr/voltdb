def test_3_get_server_count(self):
    jsonContent = self.response.json()
    print jsonContent['servers']
    self.assertTrue((len(jsonContent['servers']) == 3), ('Expected 3 servers. Found ' + str(len(jsonContent['servers']))))
