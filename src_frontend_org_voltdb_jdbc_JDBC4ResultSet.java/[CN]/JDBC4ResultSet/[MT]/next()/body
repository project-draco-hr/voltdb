{
  checkClosed();
  if (cursor_AfterLast || table.getActiveRowIndex() == row_Count - 1) {
    cursor_AfterLast=true;
    return false;
  }
  if (cursor_beforeFirst)   cursor_beforeFirst=false;
  try {
    return table.advanceRow();
  }
 catch (  Exception x) {
    throw SQLError.get(x);
  }
}
