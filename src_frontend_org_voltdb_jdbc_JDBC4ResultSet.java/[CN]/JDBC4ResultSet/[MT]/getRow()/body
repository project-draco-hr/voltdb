{
  checkClosed();
  if (cursorPosition == Position.afterLast)   return 0;
  if (cursorPosition == Position.beforeFirst)   return 0;
  try {
    return table.getActiveRowIndex() + 1;
  }
 catch (  Exception x) {
    throw SQLError.get(x);
  }
}
