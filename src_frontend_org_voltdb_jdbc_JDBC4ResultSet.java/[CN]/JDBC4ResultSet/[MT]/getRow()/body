{
  checkClosed();
  if (cursor_AfterLast)   return 0;
  if (cursor_beforeFirst)   return 0;
  try {
    return table.getActiveRowIndex() + 1;
  }
 catch (  Exception x) {
    throw SQLError.get(x);
  }
}
