{
  checkClosed();
  if (row_Count == 0)   return false;
  try {
    if (table.getActiveRowIndex() != 0)     table.resetRowPosition();
    return table.advanceToRow(0);
  }
 catch (  Exception x) {
    throw SQLError.get(x);
  }
}
