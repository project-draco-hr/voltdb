{
  checkClosed();
  try {
    if (table.getRowCount() == 0) {
      return false;
    }
    return table.advanceToRow(table.getRowCount() - 1);
  }
 catch (  Exception x) {
    throw SQLError.get(x);
  }
}
