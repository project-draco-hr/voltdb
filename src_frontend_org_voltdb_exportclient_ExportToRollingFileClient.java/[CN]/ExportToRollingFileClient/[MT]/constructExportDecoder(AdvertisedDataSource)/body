{
  String table_name=source.tableName;
  if (!m_tableDecoders.containsKey(table_name)) {
    m_tableDecoders.put(table_name,new ExportToRollingFileDecoder(source,m_nonce,m_outDir,m_escaper,m_period,m_dateformat,m_firstfield));
  }
  return m_tableDecoders.get(table_name);
}
