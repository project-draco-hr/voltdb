{
  Runnable task=null;
  while ((task=m_tasks.poll()) != null) {
    task.run();
  }
  if (readyKeys > 0) {
    optimizedInvokeCallbacks(r);
  }
  task=null;
  while ((task=m_tasks.poll()) != null) {
    task.run();
  }
}
