{
  long currentIJCount=histogram.getCountAt(currentBucketIndex,currentSubBucketIndex);
  return (currentIJCount != 0) && ((visitedSubBucketIndex != currentSubBucketIndex) || (visitedBucketIndex != currentBucketIndex));
}
