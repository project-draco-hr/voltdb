{
  if (!testReadOnly()) {
    m_site.getNextUndoToken();
  }
  final ClientResponseImpl response=new ClientResponseImpl();
  response.setResults(ClientResponseImpl.SUCCESS,new VoltTable[]{},"MockSPVoltProcedure Response");
  ++m_called;
  return response;
}
