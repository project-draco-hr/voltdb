{
  try {
    if (s == Subject.DEFAULT)     return incomingMessages.take();
 else     return m_failureNoticeMessages.take();
  }
 catch (  InterruptedException e) {
    throw new RuntimeException(e);
  }
}
