{
  HeartbeatMessage hbm=new HeartbeatMessage(initiator_id,txn_id,safe_txn_id);
  int[] upExecutionSites=m_voltdb.getCatalogContext().siteTracker.getUpExecutionSites();
  for (  int i : upExecutionSites) {
    m_mboxes[i].deliver(hbm);
  }
}
