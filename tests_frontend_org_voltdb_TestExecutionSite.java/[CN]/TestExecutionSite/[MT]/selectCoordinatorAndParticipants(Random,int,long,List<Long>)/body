{
  long coordinator=getSiteIdForInitiatorId(initiator);
  for (int i=0; i < SITE_COUNT; i++) {
    if (i == coordinator)     continue;
 else     participants.add(getHSIdForES(i));
  }
  return coordinator;
}
