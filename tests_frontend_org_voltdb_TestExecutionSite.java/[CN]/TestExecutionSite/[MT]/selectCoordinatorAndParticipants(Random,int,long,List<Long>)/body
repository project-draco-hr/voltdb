{
  long coordinator=getSiteIdForInitiatorId(initiator);
  for (int i=0; i < SITE_COUNT; i++) {
    long hsId=getHSIdForES(i);
    if (hsId == coordinator)     continue;
 else     participants.add(hsId);
  }
  return coordinator;
}
