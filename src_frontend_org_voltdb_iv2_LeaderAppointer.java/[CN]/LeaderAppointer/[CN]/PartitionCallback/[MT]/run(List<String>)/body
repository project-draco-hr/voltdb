{
  tmLog.debug("Handling babysitter callback for partition " + m_partitionId + ": children: "+ CoreUtils.hsIdCollectionToString(VoltZK.childrenToReplicaHSIds(children)));
  if (m_inStartup.get()) {
    if (children.size() == (m_kfactor + 1)) {
      assignLeader(m_partitionId,VoltZK.childrenToReplicaHSIds(children));
    }
  }
}
