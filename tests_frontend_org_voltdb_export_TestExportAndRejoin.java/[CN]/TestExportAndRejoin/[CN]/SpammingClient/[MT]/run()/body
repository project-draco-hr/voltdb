{
  try {
    while (m_continue.get()) {
      if (!m_up) {
        runConnect();
      }
 else {
        runOperation();
      }
    }
  }
 catch (  AssertionError e) {
    e.printStackTrace();
    globalContinue.set(false);
  }
}
