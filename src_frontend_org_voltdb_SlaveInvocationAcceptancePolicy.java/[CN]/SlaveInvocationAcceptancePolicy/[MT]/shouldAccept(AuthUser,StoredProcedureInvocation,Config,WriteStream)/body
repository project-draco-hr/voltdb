{
  if (invocation == null || sysProc == null) {
    return false;
  }
  if (invocation.getType() == ProcedureInvocationType.ORIGINAL && sysProc.allowedInSlave && !invocation.procName.equalsIgnoreCase("@AdHoc")) {
    return true;
  }
  return shouldAccept(user,invocation,sysProc.readOnly,s);
}
