{
  voltQueueSQL(insert,w_id,w_id,w_id);
  try {
    voltQueueSQL(insert,w_id,w_id,w_id);
    voltExecuteSQL();
  }
 catch (  SQLException e) {
  }
  voltQueueSQL(select);
  return voltExecuteSQL();
}
