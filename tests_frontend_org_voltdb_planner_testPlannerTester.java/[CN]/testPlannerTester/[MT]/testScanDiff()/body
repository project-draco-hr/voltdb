{
  assertTrue(compileDiffMatchPattern("select * from t where a = ?;",3,true,"select * from l,t where l.a = t.a order by b limit ?;",3,true,"Scan time diff :","(1 => 2)","Table diff at leaf 0:","(INDEXSCAN on T => SEQSCAN on L)","Table diff at leaf 1:","([] => INDEXSCAN on T)"));
}
