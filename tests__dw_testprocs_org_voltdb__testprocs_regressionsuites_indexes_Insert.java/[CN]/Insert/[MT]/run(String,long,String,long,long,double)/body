{
  if (tablename.equals("P1")) {
    voltQueueSQL(i_p1,(int)id,desc,(int)num,(int)num2,ratio);
  }
 else   if (tablename.equals("R1")) {
    voltQueueSQL(i_r1,(int)id,desc,(int)num,(int)num2,ratio);
  }
 else   if (tablename.equals("P2")) {
    voltQueueSQL(i_p2,(int)id,desc,(int)num,(int)num2,ratio);
  }
 else   if (tablename.equals("R2")) {
    voltQueueSQL(i_r2,(int)id,desc,(int)num,(int)num2,ratio);
  }
 else   if (tablename.equals("P3")) {
    voltQueueSQL(i_p3,(int)id,desc,(int)num,(int)num2,ratio);
  }
 else   if (tablename.equals("R3")) {
    voltQueueSQL(i_r3,(int)id,desc,(int)num,(int)num2,ratio);
  }
  return voltExecuteSQL();
}
