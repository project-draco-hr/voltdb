{
  return String.format("Unable to create %sview %s %sbecause it uses unsafe operations.",((singleTableName != null) ? "single table " : "multi-table "),viewName,((singleTableName == null) ? "" : String.format("on table %s ",singleTableName)));
}
