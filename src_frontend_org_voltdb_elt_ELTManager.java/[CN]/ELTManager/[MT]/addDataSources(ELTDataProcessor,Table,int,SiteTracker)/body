{
  ArrayList<Integer> sites=siteTracker.getLiveExecutionSitesForHost(hostId);
  for (  Integer site : sites) {
    newProcessor.addDataSource(new ELTDataSource("database",table.getTypeName(),siteTracker.getPartitionForSite(site),site,table.getRelativeIndex(),table.getColumns()));
  }
}
