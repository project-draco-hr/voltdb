{
  int size=ol_o_id.length;
  for (int i=0; i < size; ++i) {
    voltQueueSQL(insert,ol_o_id[i],ol_d_id[i],ol_w_id,ol_number[i],ol_i_id[i],ol_supply_w_id[i],ol_delivery_d[i],ol_quantity[i],ol_amount[i],ol_dist_info[i]);
  }
  return voltExecuteSQL();
}
