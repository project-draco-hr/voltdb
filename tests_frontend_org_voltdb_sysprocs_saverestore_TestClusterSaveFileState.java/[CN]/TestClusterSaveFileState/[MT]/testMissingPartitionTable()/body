{
  addPartitionToTestData(0,PART_TABLE_NAME_1,0,0,3);
  addPartitionToTestData(1,PART_TABLE_NAME_1,1,1,3);
  try {
    @SuppressWarnings("unused") ClusterSaveFileState state=new ClusterSaveFileState(m_siteInput);
  }
 catch (  IOException e) {
    assertTrue(true);
    return;
  }
  assertTrue(false);
}
