{
  addReplicatedTableToTestData(0,REPL_TABLE_NAME_1);
  addBadSiteToTestData("BAD_CLUSTER",DATABASE_NAME);
  try {
    @SuppressWarnings("unused") ClusterSaveFileState state=new ClusterSaveFileState(m_siteInput,m_allowELT);
  }
 catch (  IOException e) {
    assertTrue(true);
    return;
  }
  assertTrue(false);
}
