{
  try {
    @SuppressWarnings("unused") ClusterSaveFileState state=new ClusterSaveFileState(m_siteInput,m_allowExport);
  }
 catch (  IOException e) {
    assertTrue(true);
    return;
  }
  assertTrue(false);
}
