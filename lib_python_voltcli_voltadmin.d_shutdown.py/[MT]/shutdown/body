@VOLT.Command(wrapper=VOLT.AdminWrapper(), description='Shut down the running VoltDB cluster.')
def shutdown(runner):
    response = runner.call_proc('@Shutdown', [], [], check_status=False)
    print response
