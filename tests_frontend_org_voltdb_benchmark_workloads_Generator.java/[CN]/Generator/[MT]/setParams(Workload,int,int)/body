{
  String type=myWL.paramTypes[procIndex][paramIndex];
  GeneratorType generator=myWL.generatorTypes[procIndex][paramIndex];
  Object randVal=null;
  if (type.startsWith("VARCHAR(")) {
    int index=type.indexOf(')');
    String temp=type.substring(8,index);
    int length=Integer.valueOf(temp);
switch (generator) {
case RANDOM:
      randVal=RandomValues.getString(length);
    break;
}
}
 else if (type.equals("FLOAT")) {
switch (generator) {
case RANDOM:
  randVal=RandomValues.getDouble();
break;
}
}
 else if (type.equals("DECIMAL")) {
switch (generator) {
case RANDOM:
randVal=RandomValues.getBigDecimal();
break;
}
}
 else if (type.equals("BIGINT")) {
switch (generator) {
case RANDOM:
randVal=RandomValues.getLong();
break;
}
}
 else if (type.equals("INTEGER")) {
switch (generator) {
case RANDOM:
randVal=RandomValues.getInt();
break;
}
}
 else if (type.equals("SMALLINT")) {
switch (generator) {
case RANDOM:
randVal=RandomValues.getShort();
break;
}
}
 else if (type.equals("TINYINT")) {
switch (generator) {
case RANDOM:
randVal=RandomValues.getByte();
break;
}
}
 else if (type.equals("TIMESTAMP")) {
switch (generator) {
case RANDOM:
randVal=RandomValues.getTimestamp();
break;
}
}
 else ;
myWL.params[procIndex][paramIndex]=randVal;
}
