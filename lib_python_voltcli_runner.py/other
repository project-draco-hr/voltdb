__author__ = 'scooper'
import sys
import os
import copy
import optparse
import inspect
import re
from voltcli import cli
from voltcli import environment
from voltcli import utility
base_cli_spec = cli.CLISpec(description='Specific actions are provided by subcommands.  Run "%prog help SUBCOMMAND" to\ndisplay full usage for a subcommand, including its options and arguments.  Note\nthat some subcommands are fully handled in Java.  For these subcommands help is\nusually available by running the subcommand with no additional arguments or\nfollowed by the -h option.\n', usage='%prog [OPTIONS] COMMAND [ARGUMENTS ...]', cli_options=(cli.CLIBoolean('-d', '--debug', 'debug', 'display debug messages'), cli.CLIBoolean('-n', '--dry-run', 'dryrun', 'display actions without executing them'), cli.CLIBoolean('-p', '--pause', 'pause', 'pause before significant actions'), cli.CLIBoolean('-v', '--verbose', 'verbose', 'display verbose messages, including external command lines')))
internal_commands = ['volt', 'voltadmin']
