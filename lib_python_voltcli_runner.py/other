__author__ = 'scooper'
import sys
import os
import inspect
import voltdbclient
from verbs import *
from voltcli import cli
from voltcli import environment
from voltcli import utility
base_cli_spec = cli.CLISpec(description='Specific actions are provided by verbs.  Run "%prog help VERB" to display full\nusage for a verb, including its options and arguments.\n', usage='%prog VERB [ ARGUMENTS ... ]', options=(cli.BooleanOption(None, '--debug', 'debug', 'display debug messages'), cli.BooleanOption(None, '--pause', 'pause', None), cli.BooleanOption('-v', '--verbose', 'verbose', 'display verbose messages, including external command lines')))
internal_commands = ['volt', 'voltadmin']
