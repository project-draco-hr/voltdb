{
  if (!_connection.isIncluding()) {
    _connection.getResponseFields().putLongField(name,value);
    if (HttpHeaders.CONTENT_LENGTH.equalsIgnoreCase(name))     _connection._generator.setContentLength(value);
  }
}
