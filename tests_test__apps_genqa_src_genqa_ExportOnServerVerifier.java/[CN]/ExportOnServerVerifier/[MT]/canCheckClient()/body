{
  boolean can_check=true;
  for (int i=0; i < m_partitions; i++) {
    if (m_rowTxnIds.get(i).size() - m_checkedUpTo.get(i) <= 0) {
      can_check=false;
      break;
    }
  }
  return can_check;
}
