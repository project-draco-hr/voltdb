{
  if (m_state != QueueState.BLOCKED_EMPTY)   return 0;
  long txnId=Long.MAX_VALUE;
  for (  LastInitiatorData lid : m_initiatorData.values()) {
    if (txnId > lid.m_lastSeenTxnId)     txnId=lid.m_lastSeenTxnId;
  }
  txnId=Math.max(0,txnId);
  return txnId;
}
