{
  assert(name != null);
  this.roles=roles;
  this.cls=null;
  this.name=name;
  if (sql.endsWith(";")) {
    this.sql=sql;
  }
 else {
    this.sql=sql + ";";
  }
  this.partitionInfo=partitionInfo;
  assert(this.name != null);
}
