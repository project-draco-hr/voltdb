{
  long len=parseNext();
  long total=len > 0 ? len : 0;
  while (!isComplete() && _buffer != null && _buffer.length() > 0) {
    len=parseNext();
    if (len > 0)     total+=len;
  }
  return total;
}
