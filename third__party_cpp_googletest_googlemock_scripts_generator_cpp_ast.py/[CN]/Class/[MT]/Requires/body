def Requires(self, node):
    if self.bases:
        for token_list in self.bases:
            for token in token_list:
                if (token.name == node.name):
                    return True
    return False
