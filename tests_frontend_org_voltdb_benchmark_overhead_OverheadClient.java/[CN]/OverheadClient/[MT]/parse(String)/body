{
  for (  Transaction t : values()) {
    if (value.equalsIgnoreCase(t.name()) || value.equalsIgnoreCase(t.displayName) || value.equalsIgnoreCase(t.callName)) {
      return t;
    }
  }
  throw new IllegalArgumentException("No measure overhead transaction found.");
}
