{
  if ((transactionToInvoke == Transaction.MEASURE_OVERHEAD) || (transactionToInvoke == Transaction.MEASURE_OVERHEAD_MULTIPARTITION) || (transactionToInvoke == Transaction.MEASURE_OVERHEAD_MULTIPARTITION_BATCHED)|| (transactionToInvoke == Transaction.MEASURE_OVERHEAD_MULTIPARTITION_NOFINAL)|| (transactionToInvoke == Transaction.MEASURE_OVERHEAD_MULTIPARTITION_TWOSTMTS)) {
    return m_voltClient.callProcedure(new MeasureOverheadCallback(),transactionToInvoke.callName,new Object[]{new Long(invocations++)});
  }
 else {
    Object argObjects[]=new Object[42];
    argObjects[0]=invocations++;
    if (transactionToInvoke == Transaction.MEASURE_OVERHEAD_42_LONGS)     for (int ii=1; ii < argObjects.length; ii++) {
      argObjects[ii]=new Long(ii);
    }
    if ((transactionToInvoke == Transaction.MEASURE_OVERHEAD_42_STRINGS) || (transactionToInvoke == Transaction.MEASURE_OVERHEAD_MULTIPARTITION_42_STRINGS))     for (int ii=1; ii < argObjects.length; ii++) {
      argObjects[ii]=new String("1234567890");
    }
 else     assert(false);
    return m_voltClient.callProcedure(new MeasureOverheadCallback(),transactionToInvoke.callName,argObjects);
  }
}
