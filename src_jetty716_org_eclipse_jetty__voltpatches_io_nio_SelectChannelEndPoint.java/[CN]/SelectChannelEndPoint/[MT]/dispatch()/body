{
synchronized (this) {
    if (_dispatched)     _redispatched=true;
 else {
      _dispatched=_manager.dispatch(this);
      if (!_dispatched) {
        Log.warn("Dispatched Failed!");
        updateKey();
      }
    }
  }
}
