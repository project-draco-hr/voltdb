{
  ClientResponseImpl resp=new ClientResponseImpl();
  try {
    resp.initFromBuffer(b);
  }
 catch (  IOException ioe) {
    LOG.error("Unable to deserialize ClientResponse from snapshot",ioe);
    return;
  }
  handleResponse((ClientResponse)resp);
}
