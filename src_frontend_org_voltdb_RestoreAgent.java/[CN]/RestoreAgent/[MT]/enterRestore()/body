{
  try {
    m_leaderElector=new LeaderElector(m_zk,VoltZK.restore_barrier,new byte[0],null);
  }
 catch (  Exception e) {
    VoltDB.crashGlobalVoltDB("Failed to create Zookeeper node: " + e.getMessage(),false,e);
  }
}
