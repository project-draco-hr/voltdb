{
  voltQueueSQL(getCustomersByLastName,c_last,c_d_id,c_w_id);
  final VoltTable customers=voltExecuteSQL()[0];
  final int namecnt=customers.getRowCount();
  if (namecnt == 0) {
    String c_last_str="";
    try {
      c_last_str=new String(c_last,"UTF-8");
    }
 catch (    UnsupportedEncodingException e) {
    }
    throw new VoltAbortException("no customers with last name: " + c_last_str + " in warehouse: "+ c_w_id+ " and in district "+ c_d_id);
  }
  return null;
}
