def __repr__(self):
    self.__setup()
    if (len(self.__lines) <= self.MAXLINES):
        return '\n'.join(self.__lines)
    else:
        return ('Type %s() to see the full %s text' % ((self.__name,) * 2))
