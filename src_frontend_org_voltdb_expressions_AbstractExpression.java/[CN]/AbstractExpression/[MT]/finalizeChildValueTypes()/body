{
  if (m_left != null)   m_left.finalizeValueTypes();
  if (m_right != null)   m_right.finalizeValueTypes();
  if (m_args != null) {
    for (    AbstractExpression argument : m_args) {
      argument.finalizeValueTypes();
    }
  }
}
