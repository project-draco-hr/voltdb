{
  if (m_left != null)   m_left.resolveForDB(db);
  if (m_right != null)   m_right.resolveForDB(db);
  if (m_args != null) {
    for (    AbstractExpression argument : m_args) {
      argument.resolveForDB(db);
    }
  }
}
