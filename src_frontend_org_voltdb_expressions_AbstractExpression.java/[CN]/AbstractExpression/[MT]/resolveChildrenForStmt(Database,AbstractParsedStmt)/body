{
  if (m_left != null)   m_left.resolveForStmt(db,stmt);
  if (m_right != null)   m_right.resolveForStmt(db,stmt);
  if (m_args != null) {
    for (    AbstractExpression argument : m_args) {
      argument.resolveForStmt(db,stmt);
    }
  }
}
