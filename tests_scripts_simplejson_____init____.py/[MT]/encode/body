def encode(obj):
    '\n    demjson, python-cjson compatibility hook. Use dumps(s) instead.\n    '
    import warnings
    warnings.warn('simplejson.dumps(s) should be used instead of encode(s)', DeprecationWarning)
    return dumps(obj)
