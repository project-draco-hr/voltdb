def __init__(self, f):
    self.tar = tarfile.open(f)
    if (not re.match('.*\\d{4}-\\d{2}-\\d{2}-\\d{2}:\\d{2}:\\d{2}\\.\\d{6}\\.tgz', f)):
        raise IOError((f + " isn't a valid logfile package"))
