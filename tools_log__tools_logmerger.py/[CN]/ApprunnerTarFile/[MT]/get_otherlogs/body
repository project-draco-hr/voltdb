def get_otherlogs(self):
    patterns = ['apprunner.log', '\\.Benchmark\\.', 'VoltDBReplicationAgent\\.(?!jstack)']
    pat = '|'.join(patterns)
    return [f for f in self.tar.getnames() if re.search(pat, f)]
