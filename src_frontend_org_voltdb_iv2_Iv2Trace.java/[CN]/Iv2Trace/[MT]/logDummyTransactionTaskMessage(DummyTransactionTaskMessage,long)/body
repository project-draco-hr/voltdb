{
  if (IV2_TRACE_ENABLED) {
    String logmsg="rxDummyTxnMsg %s from %s txnId %s spHandle %s";
    iv2log.trace(String.format(logmsg,CoreUtils.hsIdToString(localHSId),CoreUtils.hsIdToString(dtask.m_sourceHSId),txnIdToString(dtask.getTxnId()),txnIdToString(dtask.getSpHandle())));
  }
}
