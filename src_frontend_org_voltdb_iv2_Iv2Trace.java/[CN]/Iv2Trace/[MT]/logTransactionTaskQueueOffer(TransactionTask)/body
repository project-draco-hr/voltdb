{
  if (iv2queuelog.isTraceEnabled()) {
    String logmsg=new String("txnQOffer txnId %s spHandle %s type %s");
    iv2queuelog.trace(String.format(logmsg,task.getTxnId(),task.getSpHandle(),task.m_txn.isSinglePartition() ? "SP" : "MP"));
  }
}
