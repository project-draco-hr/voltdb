{
  if (iv2log.isTraceEnabled()) {
    String logmsg=new String("rxSntlMsg %s from %s txnId %s");
    iv2log.trace(String.format(logmsg,CoreUtils.hsIdToString(localHSId),CoreUtils.hsIdToString(message.m_sourceHSId),txnIdToString(txnId)));
  }
}
