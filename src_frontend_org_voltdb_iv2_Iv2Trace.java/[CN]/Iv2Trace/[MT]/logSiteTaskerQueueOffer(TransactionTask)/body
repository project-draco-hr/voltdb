{
  if (iv2queuelog.isTraceEnabled()) {
    String logmsg=new String("tskQOffer txnId %s spHandle %s type %s");
    iv2queuelog.trace(String.format(logmsg,task.getMpTxnId(),task.getLocalTxnId(),task.m_txn.isSinglePartition() ? "SP" : "MP"));
  }
}
