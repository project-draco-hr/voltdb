{
  final SyncCallback cb=new SyncCallback();
  cb.setArgs(parameters);
  final ProcedureInvocation invocation=new ProcedureInvocation(txnId,originalTxnId,m_handle.getAndIncrement(),procName,parameters);
  return callProcedure(cb,invocation);
}
