{
  if (batchTimeout < 0) {
    throw new IllegalArgumentException("Timeout value can't be negative.");
  }
  return callProcedureWithClientTimeout(callback,batchTimeout,procName,Distributer.USE_DEFAULT_CLIENT_TIMEOUT,TimeUnit.NANOSECONDS,parameters);
}
