def initialize(self):
    self.verbs.extend(vcli_util.find_and_load_subclasses(VOLT.Verb, self.search_dirs, self.verbs_subdir, VOLT=VOLT, Command=self.decorator_Command, Java_Command=self.decorator_Java_Command))
    self.verbs.sort(cmp=(lambda x, y: cmp(x.name, y.name)))
