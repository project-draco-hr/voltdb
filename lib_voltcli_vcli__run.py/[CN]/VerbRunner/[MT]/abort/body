def abort(self, *msgs):
    '\n        Display errors (optional) and abort execution.\n        '
    vcli_util.error(('Fatal error in "%s" command.' % self.name), *msgs)
    self.help()
    vcli_util.abort()
