def help(self, *args):
    for name in args:
        for verb in vcli_meta.verbs:
            if (verb.name == name):
                print ''
                self.verb_parser.print_help()
                print ''
            else:
                error(('Verb "%s" was not found.' % (name, self.name)))
