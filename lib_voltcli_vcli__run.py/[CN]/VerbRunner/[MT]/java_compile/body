def java_compile(self, outdir, *srcfiles):
    '\n        Compile Java source using javac.\n        '
    vcli_util.run_cmd('javac', '-target', '1.6', '-source', '1.6', '-classpath', self.classpath, '-d', outdir, *srcfiles)
