def go(self, *args):
    '\n        Default go action provided by Verb object.\n        '
    if (self.go_default is None):
        vcli_util.abort(('Verb "%s" (class %s) does not provide a default go action.' % (self.verb.name, self.verb.__class__.__name__)))
    else:
        self.go_default(self, *args)
