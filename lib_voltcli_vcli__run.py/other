__author__ = 'scooper'
import sys
import os
import copy
import optparse
import inspect
import vcli_cli
import vcli_env
import vcli_util
base_cli_spec = vcli_cli.CLISpec(description='Specific actions are provided by subcommands.  Run "%prog help SUBCOMMAND" to\ndisplay full usage for a subcommand, including its options and arguments.  Note\nthat some subcommands are fully handled in Java.  For these subcommands help is\nusually available by running the subcommand with no additional arguments or\nfollowed by the -h option.\n', usage='%prog [OPTIONS] COMMAND [ARGUMENTS ...]', cli_options=(vcli_cli.CLIBoolean('-d', '--debug', 'debug', 'display debug messages'), vcli_cli.CLIBoolean('-n', '--dry-run', 'dryrun', 'display actions without executing them'), vcli_cli.CLIBoolean('-p', '--pause', 'pause', 'pause before significant actions'), vcli_cli.CLIBoolean('-v', '--verbose', 'verbose', 'display verbose messages, including external command lines')))
internal_commands = ['volt', 'voltadmin']
