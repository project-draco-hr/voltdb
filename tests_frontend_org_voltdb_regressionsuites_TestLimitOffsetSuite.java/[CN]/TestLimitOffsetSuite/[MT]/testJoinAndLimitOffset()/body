{
  Client client=this.getClient();
  load(client);
  VoltTable result=client.callProcedure("@AdHoc","SELECT * FROM A, B WHERE A.PKEY < B.PKEY LIMIT 1 OFFSET 1;").getResults()[0];
  assertEquals(1,result.getRowCount());
}
