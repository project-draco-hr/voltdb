{
  if (m_message == null) {
    return true;
  }
  try {
    if (m_schema != null) {
      if (!send(m_schema)) {
        return false;
      }
    }
    return send(m_message);
  }
  finally {
    discard();
  }
}
