{
  if (rejoinLog.isTraceEnabled()) {
    rejoinLog.trace("Discarding buffer at index " + String.valueOf(m_blockIndex));
  }
  if (m_message != null) {
    m_message.discard();
    m_message=null;
  }
  if (m_schema != null) {
    m_schema.discard();
    m_schema=null;
  }
}
