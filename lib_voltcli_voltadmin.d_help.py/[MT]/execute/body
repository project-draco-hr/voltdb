@VOLT.Command(description='Display command help.', usage='[COMMAND ...]')
def execute(runner):
    if runner.args:
        for arg in runner.args:
            runner.help(arg)
    else:
        runner.usage()
