{
  sourceResultSet.checkColumnBounds(column);
  VoltType type=sourceResultSet.table.getColumnType(column);
switch (type) {
case TINYINT:
case SMALLINT:
case INTEGER:
case BIGINT:
case FLOAT:
case DECIMAL:
case TIMESTAMP:
    return false;
case STRING:
  return true;
default :
return true;
}
}
