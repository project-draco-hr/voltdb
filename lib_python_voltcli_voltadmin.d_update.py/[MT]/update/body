@VOLT.Client(description='Update the schema of a running database.', arguments=(VOLT.StringArgument('catalog', 'the new application catalog jar file path'), VOLT.StringArgument('deployment', 'the deployment configuration file path')))
def update(runner):
    catalog = VOLT.utility.File(runner.opts.catalog).read_hex()
    deployment = VOLT.utility.File(runner.opts.deployment).read()
    runner.call_sysproc('@UpdateApplicationCatalog', [VOLT.FastSerializer.VOLTTYPE_STRING, VOLT.FastSerializer.VOLTTYPE_STRING], [catalog, deployment])
    utility.info('The catalog update succeeded.')
