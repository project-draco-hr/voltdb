{
  try {
    client.callProcedure("NonDeterministicSPProc",0,NonDeterministicSPProc.MISMATCH_VALUES);
    fail();
  }
 catch (  ProcCallException e) {
    assertTrue(e.getMessage().contains("Connection to database"));
  }
}
