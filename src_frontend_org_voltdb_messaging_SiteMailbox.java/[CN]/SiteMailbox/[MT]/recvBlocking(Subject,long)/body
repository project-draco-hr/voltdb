{
  try {
    final Queue<VoltMessage> dq=m_messages.get(s.getId());
    assert(dq != null);
synchronized (dq) {
      if (dq.isEmpty()) {
        dq.wait(timeout);
      }
      return dq.poll();
    }
  }
 catch (  InterruptedException e) {
  }
  return null;
}
