{
  String[] subs={"BAT","CAT","DAT","EAT","FAT","GAT","HAT","MAT","NAT","PAT","RAT","SAT","TAT","VAT"};
  StringBuilder sb=new StringBuilder();
  for (int i=0; i < 40; i++) {
    sb.append(subs[m_rand.nextInt(13)]);
  }
  return sb.toString();
}
