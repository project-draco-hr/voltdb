{
  acquirePermit();
  return m_es.submit(new Callable<T>(){
    @Override public T call() throws Exception {
      try {
        return ood.operate(m_deque);
      }
  finally {
        releasePermit();
      }
    }
  }
);
}
