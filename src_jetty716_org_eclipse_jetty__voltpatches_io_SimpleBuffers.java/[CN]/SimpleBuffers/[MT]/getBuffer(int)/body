{
synchronized (this) {
    if (_header != null && _header.capacity() == size)     return getHeader();
    if (_buffer != null && _buffer.capacity() == size)     return getBuffer();
    return null;
  }
}
