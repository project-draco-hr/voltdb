{
  return runExportDataSourceRunner((new Runnable(){
    @Override public void run(){
      try {
        m_committedBuffers.close();
        m_shutdown=true;
      }
 catch (      IOException e) {
        exportLog.error(e);
      }
 finally {
        m_es.shutdown();
      }
    }
  }
));
}
