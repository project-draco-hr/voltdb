@Command(description='Run the VoltDB compiler to build the catalog.', usage='CLASSPATH PROJECT JAR')
def compile(runner):
    runner.java('org.voltdb.compiler.VoltCompiler', None, runner.project_path, runner.get_catalog(), *runner.args)
