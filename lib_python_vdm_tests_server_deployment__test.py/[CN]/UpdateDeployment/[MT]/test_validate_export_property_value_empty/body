def test_validate_export_property_value_empty(self):
    'ensure property value is not empty'
    response = requests.put(__url__, json={'export': {'configuration': [{'enabled': False, 'type': 'KAFKA', 'exportconnectorclass': 'test', 'property': [{'name': 'test', }], }], }, })
    value = response.json()
    self.assertEqual(value['success'], False)
    self.assertEqual(response.status_code, 200)
