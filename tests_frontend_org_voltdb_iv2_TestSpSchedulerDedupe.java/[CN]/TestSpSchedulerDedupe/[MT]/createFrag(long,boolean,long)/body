{
  FragmentTaskMessage frag=new FragmentTaskMessage(destHSId,destHSId,txnId,System.currentTimeMillis(),readOnly,false,false);
  frag.setSpHandleAndSpUniqueId(TxnEgo.makeZero(0).getTxnId(),ZERO_UNIQUE_ID);
  return frag;
}
