{
  if (!(peer_ instanceof SyncResponse)) {
    return false;
  }
  if (peer_ == this) {
    return true;
  }
  SyncResponse peer=(SyncResponse)peer_;
  boolean ret=false;
  ret=path.equals(peer.path);
  if (!ret)   return ret;
  return ret;
}
