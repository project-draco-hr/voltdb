{
  try {
    Thread.sleep(m_delay);
  }
 catch (  InterruptedException e) {
  }
  VoltTable next=m_schema.clone(2048 * 1024);
  for (int i=0; i < m_blockSize; i++) {
    if (!addRow(next))     return next;
  }
  return next;
}
