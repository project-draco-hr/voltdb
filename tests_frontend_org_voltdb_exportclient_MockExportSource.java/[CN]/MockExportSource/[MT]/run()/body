{
  try {
    ServerSocket listener=new ServerSocket(VoltDB.DEFAULT_PORT);
    while (true) {
      Socket sock=listener.accept();
      acceptThread(sock);
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
}
