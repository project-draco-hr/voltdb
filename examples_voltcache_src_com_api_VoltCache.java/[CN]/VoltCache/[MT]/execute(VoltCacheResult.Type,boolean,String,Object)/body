{
  try {
    if (noreply)     return this.Connection.executeAsync(VoltCache.NullCallback,procedure,parameters) ? VoltCacheResult.SUBMITTED : VoltCacheResult.ERROR;
 else     return VoltCacheResult.get(type,this.Connection.execute(procedure,parameters));
  }
 catch (  Exception x) {
    return VoltCacheResult.ERROR;
  }
}
