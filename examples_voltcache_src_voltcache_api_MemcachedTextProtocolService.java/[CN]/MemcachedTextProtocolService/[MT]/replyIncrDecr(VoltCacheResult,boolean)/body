{
  if (noreply)   this.replyStatus(response);
 else {
    if (response.code == VoltCacheResult.NOT_FOUND)     this.reply(RESPONSE_NOT_FOUND);
 else     this.reply(Long.toString(response.incrDecrValue) + "\r\n");
  }
}
