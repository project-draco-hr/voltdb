{
  if (noreply)   this.replyStatus(response);
 else {
    long value=response.IncrDecrValue;
    if (value == VoltType.NULL_BIGINT)     this.reply(RESPONSE_NOT_FOUND);
 else     this.reply(Long.toString(value) + "\r\n");
  }
}
