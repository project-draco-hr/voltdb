{
  Thread t=new Thread(){
    @Override public void run(){
      MockExportSource.run(1,1,0);
    }
  }
;
  t.start();
  ExportToFileClient etfc=new ExportToFileClient(',',"N",new File("."),1,"yyyyMMddHHmmss",null,0,false,false,false,0,ExportToFileClient.BinaryEncoding.HEX);
  InetSocketAddress addr=new InetSocketAddress("127.0.0.1",VoltDB.DEFAULT_PORT);
  etfc.addServerInfo(addr);
  etfc.run();
}
