{
  Thread t=new Thread(){
    @Override public void run(){
      MockExportSource.run();
    }
  }
;
  t.start();
  ExportToFileClient etfc=new ExportToFileClient(',',"N",new File("."),1,"yyyyMMddHHmmss",0,false);
  InetSocketAddress addr=new InetSocketAddress("127.0.0.1",VoltDB.DEFAULT_PORT);
  etfc.addServerInfo(addr);
  etfc.run();
}
