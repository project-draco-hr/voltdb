{
  if (DEBUG) {
    System.out.println();
    for (    int id : rowIds) {
      System.out.println("JSON document (DOC column), for id=" + id + " ("+ description+ "):\n"+ getDocFromId(client,id));
    }
  }
}
