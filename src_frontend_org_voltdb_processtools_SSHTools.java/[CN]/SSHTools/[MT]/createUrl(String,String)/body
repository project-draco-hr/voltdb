{
  String url=new String();
  if (m_username != null)   url=url.concat(m_username + "@");
  if (hostname != null)   url=url.concat(hostname);
  url=url.concat(":");
  if (path != null)   url=url.concat(path);
  return url;
}
