{
  StringBuilder sb=new StringBuilder();
  if (m_username != null)   sb.append(m_username + "@");
  if (hostname != null)   sb.append(hostname);
  sb.append(":");
  if (path != null)   sb.append(path);
  return sb.toString();
}
