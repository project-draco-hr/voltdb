{
synchronized (m_catalogUpdateLock) {
    m_catalogContext=m_catalogContext.update(m_catalogContext.m_transactionId,null,diffCommands,false,-1);
  }
  for (  ClientInterface ci : m_clientInterfaces) {
    ci.notifyOfCatalogUpdate();
  }
}
