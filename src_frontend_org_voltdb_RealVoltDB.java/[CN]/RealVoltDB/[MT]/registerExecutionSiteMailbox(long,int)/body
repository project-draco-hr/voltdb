{
  JSONObject jsObj=new JSONObject();
  jsObj.put("HSId",HSId);
  jsObj.put("partitionId",partitionId);
  byte[] payload=jsObj.toString(4).getBytes("UTF-8");
  m_messenger.getZK().create(VoltZK.mailboxes_executionsites_site,payload,Ids.OPEN_ACL_UNSAFE,CreateMode.EPHEMERAL_SEQUENTIAL);
}
