{
  JSONObject jsObj=new JSONObject();
  jsObj.put("HSId",HSId);
  byte[] payload=jsObj.toString(4).getBytes("UTF-8");
  m_messenger.getZK().create("/mailboxes/initiators/initiator",payload,Ids.OPEN_ACL_UNSAFE,CreateMode.EPHEMERAL_SEQUENTIAL);
}
