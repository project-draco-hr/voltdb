{
synchronized (m_periodicWorkThread) {
    if (delay > 0) {
      m_periodicWorkThread.scheduleWithFixedDelay(work,initialDelay,delay,unit);
    }
 else {
      m_periodicWorkThread.schedule(work,initialDelay,unit);
    }
  }
}
