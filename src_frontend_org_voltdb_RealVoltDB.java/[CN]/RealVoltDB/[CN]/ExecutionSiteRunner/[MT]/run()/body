{
  m_siteObj=new ExecutionSite(m_siteId,m_srcCatalog);
  m_srcCatalog=null;
synchronized (this) {
    m_isSiteCreated=true;
    this.notifyAll();
    try {
      wait();
    }
 catch (    InterruptedException e) {
      throw new RuntimeException(e);
    }
  }
  m_siteObj.run();
}
