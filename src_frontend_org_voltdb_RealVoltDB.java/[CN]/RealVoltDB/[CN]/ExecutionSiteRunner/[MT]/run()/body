{
  m_siteObj=new ExecutionSite(m_siteId,m_context,m_serializedCatalog);
synchronized (this) {
    m_isSiteCreated=true;
    this.notifyAll();
    try {
      wait();
    }
 catch (    InterruptedException e) {
      throw new RuntimeException(e);
    }
  }
  m_siteObj.run();
}
