{
  FragmentTaskMessage msg=mock(FragmentTaskMessage.class);
  when(msg.getTxnId()).thenReturn(mpTxnId);
  FragmentTask task=new FragmentTask(null,(ParticipantTransactionState)txn,msg);
  return task;
}
