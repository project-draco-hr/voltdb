{
  System.out.println("java -cp <classpath> org.voltdb.exportclient.ExportToSqClient " + "--help");
  System.out.println("java -cp <classpath> org.voltdb.exportclient.ExportToSqClient " + "--servers server1[,server2,...,serverN] " + "--connect (admin|client) "+ "--nonce file_prefix "+ "[--period batch_period_in_minutes] "+ "[--outdir target_directory] "+ "[--skipinternals] "+ "[--user export_username] "+ "[--password export_password]");
  System.exit(code);
}
