{
  if (bytesTransferredInLastSec.isEmpty())   return;
  long durationInMillis=bytesTransferredInLastSec.lastKey() - start;
  StatsPoint sp=new StatsPoint(totalRangeSize,rangeSizeMoved,rowCount,durationInMillis,count,totalBalanceTime,throughput);
  statsPoint=sp;
}
