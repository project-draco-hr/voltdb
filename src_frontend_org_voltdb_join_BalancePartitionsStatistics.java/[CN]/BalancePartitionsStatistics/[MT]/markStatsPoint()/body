{
  if (!bytesTransferredInLastSec.isEmpty()) {
    statsPoint=new StatsPoint("Point",overallStats.getStartTimeNanos(),bytesTransferredInLastSec.lastKey(),totalRangeSize,overallStats.getMovedRanges(),overallStats.getMovedRows(),throughput,overallStats.getInvocationCount(),overallStats.getInvocationTime());
  }
}
