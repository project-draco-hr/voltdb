{
  if (hasWindowedExpression()) {
    throw new PlanningErrorException("Use of both windowed RANK() and GROUP BY in a single query is not supported.");
  }
  for (  VoltXMLElement child : columnsNode.children) {
    parseGroupByColumn(child);
  }
}
