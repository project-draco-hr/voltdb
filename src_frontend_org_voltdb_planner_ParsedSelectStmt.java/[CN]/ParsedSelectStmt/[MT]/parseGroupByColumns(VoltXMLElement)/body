{
  if (hasWindowedExpression()) {
    throw new PlanningErrorException("Use of both windowed operations and GROUP BY is not supported.");
  }
  for (  VoltXMLElement child : columnsNode.children) {
    parseGroupByColumn(child);
  }
}
