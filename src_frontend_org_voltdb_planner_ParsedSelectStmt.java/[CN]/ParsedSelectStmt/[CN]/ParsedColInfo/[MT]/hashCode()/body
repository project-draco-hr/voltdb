{
  int result=0;
  if (expression != null)   result+=expression.hashCode();
  result=new HashCodeBuilder(17,31).append(alias).append(columnName).append(tableName).append(index).append(size).append(finalOutput).append(orderBy).append(ascending).append(groupBy).toHashCode();
  return result;
}
