def test_unicode_password(self):
    j = jenkins.Jenkins('{0}'.format(self.base_url), six.u('nonascii'), six.u('\xe9\\u20ac'))
    self.assertEqual(j.server, self.make_url(''))
    self.assertEqual(j.auth, 'Basic bm9uYXNjaWk6w6nigqw=')
    self.assertEqual(j.crumb, None)
