{
  assert(!((params == null) && (unserializedParams == null)));
  assert((params != null) || (unserializedParams != null));
  out.write(type.getValue());
  if (type == ProcedureInvocationType.REPLICATED) {
    out.writeLong(originalTxnId);
  }
  out.writeString(procName);
  out.writeLong(clientHandle);
  if (unserializedParams != null)   out.write(unserializedParams.array(),unserializedParams.position() + unserializedParams.arrayOffset(),unserializedParams.remaining());
 else   if (params != null) {
    out.writeObject(getParams());
  }
}
