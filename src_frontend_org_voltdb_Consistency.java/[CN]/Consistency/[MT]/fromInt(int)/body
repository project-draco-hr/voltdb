{
  if (value == FAST.value) {
    return FAST;
  }
 else   if (value == SAFE.value) {
    return SAFE;
  }
 else {
    throw new IllegalArgumentException(String.format("No Consistency.ReadLevel with value: %d",value));
  }
}
