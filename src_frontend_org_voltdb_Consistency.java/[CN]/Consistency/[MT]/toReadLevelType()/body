{
  if (this == FAST) {
    return ReadlevelType.FAST;
  }
 else   if (this == SAFE) {
    return ReadlevelType.SAFE;
  }
 else {
    throw new IllegalArgumentException(String.format("No ReadlevelType mapping for Consistency.ReadLevel: %s",toString()));
  }
}
