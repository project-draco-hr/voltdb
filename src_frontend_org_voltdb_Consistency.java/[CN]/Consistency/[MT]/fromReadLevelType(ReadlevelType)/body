{
  if (value == ReadlevelType.FAST) {
    return FAST;
  }
 else   if (value == ReadlevelType.SAFE) {
    return SAFE;
  }
 else {
    throw new IllegalArgumentException(String.format("No Consistency.ReadLevel with value: %s",value.toString()));
  }
}
