{
  while (true) {
    String line=null;
    try {
      line=br.readLine();
    }
 catch (    Exception e) {
      e.printStackTrace();
      break;
    }
    if (line == null)     break;
    System.out.println("SUBPROCESS: " + line);
  }
}
