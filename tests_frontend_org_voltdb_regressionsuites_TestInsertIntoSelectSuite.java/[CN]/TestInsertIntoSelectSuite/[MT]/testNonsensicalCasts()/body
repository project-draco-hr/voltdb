{
  final Client client=getClient();
  final long partitioningValue=5;
  initializeTables(client);
  verifyProcFails(client,"Could not convert string value","insert_p_source_p_nonsensical_cast",partitioningValue);
}
