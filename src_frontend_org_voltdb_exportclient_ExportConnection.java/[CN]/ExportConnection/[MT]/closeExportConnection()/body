{
  if (m_socket != null) {
    ;
    if (m_socket.isConnected()) {
      try {
        m_socket.close();
      }
 catch (      IOException e) {
        e.printStackTrace();
      }
    }
  }
  m_state=CLOSED;
  dataSources.clear();
}
