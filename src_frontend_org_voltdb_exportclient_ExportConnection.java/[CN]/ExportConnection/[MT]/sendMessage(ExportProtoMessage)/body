{
  ByteBuffer buf=m.toBuffer();
  long length=buf.remaining();
  while (buf.remaining() > 0) {
    m_socket.write(buf);
  }
  if (m_bandwidthMonitor != null)   m_bandwidthMonitor.logBytesTransfered(m_ipString,0,length);
}
