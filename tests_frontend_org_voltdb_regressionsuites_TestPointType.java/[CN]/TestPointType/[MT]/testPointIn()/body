{
  Client client=getClient();
  fillTable(client,0);
  Object listParam=new PointType[]{SANTA_CLARA_PT,LOWELL_PT};
  VoltTable vt=client.callProcedure("sel_in",listParam).getResults()[0];
  assertTableEquals(new Object[][]{{1,SANTA_CLARA_PT}},vt);
}
