{
  VoltTable[] results=null;
  int allowExport=0;
  try {
    client.callProcedure("@LoadMultipartitionTable",tableName,table,allowExport);
  }
 catch (  Exception ex) {
    ex.printStackTrace();
    fail("loadTable exception: " + ex.getMessage());
  }
  return results;
}
