{
  VoltTable[] results=null;
  int allowELT=0;
  try {
    client.callProcedure("@LoadMultipartitionTable",tableName,table,allowELT);
  }
 catch (  Exception ex) {
    ex.printStackTrace();
    fail("loadTable exception: " + ex.getMessage());
  }
  return results;
}
