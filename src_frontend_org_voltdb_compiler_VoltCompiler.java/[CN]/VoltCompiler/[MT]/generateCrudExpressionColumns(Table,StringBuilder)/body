{
  boolean first=true;
  int paramoffset=-1;
  for (  Column c : table.getColumns()) {
    paramoffset++;
    if (!first)     sb.append(", ");
    first=false;
    sb.append(c.getName() + " = ?");
  }
  return paramoffset;
}
