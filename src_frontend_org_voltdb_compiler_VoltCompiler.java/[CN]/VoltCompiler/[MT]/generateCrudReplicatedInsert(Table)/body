{
  StringBuilder sb=new StringBuilder();
  sb.append("INSERT INTO " + table.getTypeName() + " VALUES ");
  generateCrudColumnList(table,sb);
  sb.append(";");
  ProcedureDescriptor pd=new ProcedureDescriptor(new ArrayList<String>(),table.getTypeName() + ".insert",sb.toString(),null,null,true);
  compilerLog.info("Synthesized built-in INSERT multi-partition procedure: " + sb.toString() + " for "+ table.getTypeName());
  return pd;
}
