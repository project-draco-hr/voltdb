{
  try {
    final String catalogContents=VoltCompilerUtils.readFileFromJarfile(testJar,"catalog.txt");
    final Catalog cat=new Catalog();
    cat.execute(catalogContents);
    Database db=cat.getClusters().get("cluster").getDatabases().get("database");
    VerifyDbSchemaConsistency(db);
  }
 catch (  IOException e) {
  }
}
