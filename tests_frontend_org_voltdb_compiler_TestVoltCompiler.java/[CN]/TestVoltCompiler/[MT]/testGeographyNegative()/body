{
  badDDLAgainstSimpleSchema(".*Partition columns must be an integer or varchar type.*","create table geogs (" + "  geog geography not null" + ");"+ "partition table geogs on column geog;");
  badDDLAgainstSimpleSchema(".*GEOGRAPHY values are not currently supported as index keys.*","create table geogs (" + "  geog geography not null" + ");  "+ "create index geogidx on geogs(geog);");
  badDDLAgainstSimpleSchema(".*GEOGRAPHY values are not currently supported as index keys.*","create table geogs (" + "  geog GEOGRAPHY primary key" + ");  ");
  badDDLAgainstSimpleSchema(".*GEOGRAPHY values are not currently supported as index keys.*","create table geogs (" + "  geog geography, " + "  primary key (geog)"+ ");  ");
  badDDLAgainstSimpleSchema(".*GEOGRAPHY values are not currently supported as index keys.*","create table geogs (" + "  geog geography, " + "  constraint uniq_geog unique (geog)"+ ");  ");
  badDDLAgainstSimpleSchema(".*GEOGRAPHY values are not currently supported as index keys.*","create table geogs (" + "  geog GEOGRAPHY unique, " + ");  ");
  badDDLAgainstSimpleSchema(".*incompatible data type in conversion.*","create table geogs (" + "  geog geography default 'polygon((3.0 9.0, 3.0 0.0, 0.0 9.0, 3.0 9.0)', " + ");  ");
  badDDLAgainstSimpleSchema(".*unexpected token.*","create table geogs (" + "  geog geography " + "    default polygonfromtext('polygon((3.0 9.0, 3.0 0.0, 0.0 9.0, 3.0 9.0)'), "+ ");  ");
}
