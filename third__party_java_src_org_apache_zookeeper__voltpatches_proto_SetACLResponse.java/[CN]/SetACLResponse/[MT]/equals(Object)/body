{
  if (!(peer_ instanceof SetACLResponse)) {
    return false;
  }
  if (peer_ == this) {
    return true;
  }
  SetACLResponse peer=(SetACLResponse)peer_;
  boolean ret=false;
  ret=stat.equals(peer.stat);
  if (!ret)   return ret;
  return ret;
}
