{
  if (m_cihm.get(adapter.connectionId()) == null) {
    ClientInterfaceHandleManager cihm=ClientInterfaceHandleManager.makeThreadSafeCIHM(true,adapter,repairCallback,AdmissionControlGroup.getDummy());
    m_cihm.put(adapter.connectionId(),cihm);
  }
  return m_cihm.get(adapter.connectionId());
}
