{
  InitiatorContext context=new InitiatorContext();
  context.siteId=m_siteId;
  if (m_initiator instanceof SimpleDtxnInitiator)   ((SimpleDtxnInitiator)m_initiator).getDumpContents(context);
  return context;
}
