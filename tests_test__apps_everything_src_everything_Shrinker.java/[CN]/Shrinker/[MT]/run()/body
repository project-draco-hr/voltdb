{
  if (getMBRss() < 800) {
    try {
      Thread.sleep(5000);
    }
 catch (    InterruptedException e) {
    }
    return;
  }
  for (int i=0; i < 500; i++) {
    try {
      byte pval=(byte)(m_rand.nextInt() % 127);
      m_client.callProcedure(new Callback(),"Nibble??????",pval,200);
    }
 catch (    Exception e) {
      e.printStackTrace();
      System.exit(-1);
    }
  }
}
