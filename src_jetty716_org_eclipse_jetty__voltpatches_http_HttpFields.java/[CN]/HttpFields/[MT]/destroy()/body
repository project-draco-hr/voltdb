{
  if (_fields != null) {
    for (int i=_fields.size(); i-- > 0; ) {
      Field field=_fields.get(i);
      if (field != null) {
        _bufferMap.remove(field.getNameBuffer());
        field.destroy();
      }
    }
    _fields.clear();
  }
}
