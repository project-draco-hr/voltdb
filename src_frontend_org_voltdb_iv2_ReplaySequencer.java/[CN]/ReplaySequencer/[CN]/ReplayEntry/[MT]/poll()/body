{
  if (isReady()) {
    if (!m_servedFragment) {
      m_servedFragment=true;
      return m_firstFragment;
    }
 else {
      return m_blockedMessages.poll();
    }
  }
 else {
    return null;
  }
}
