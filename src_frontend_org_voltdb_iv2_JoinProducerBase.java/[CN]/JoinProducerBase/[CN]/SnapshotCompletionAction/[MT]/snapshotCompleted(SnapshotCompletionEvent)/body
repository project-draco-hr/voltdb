{
  if (event.nonce.equals(m_snapshotNonce)) {
    getLogger().debug(m_whoami + "counting down snapshot monitor completion. " + "Snapshot txnId is: "+ event.multipartTxnId);
    deregister();
    kickWatchdog(true);
    m_future.set(event);
  }
 else {
    getLogger().debug(m_whoami + " observed completion of irrelevant snapshot nonce: " + event.nonce);
  }
  return null;
}
