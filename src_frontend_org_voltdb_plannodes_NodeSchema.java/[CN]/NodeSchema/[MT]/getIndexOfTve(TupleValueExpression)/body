{
  SchemaColumn col=new SchemaColumn(tve.getTableName(),tve.getColumnName(),tve.getColumnAlias());
  Integer index=m_columnsMapHelper.get(col);
  if (index != null) {
    return index.intValue();
  }
  return -1;
}
