{
  checkClosed();
  if (direction != JDBCResultSet.FETCH_FORWARD && direction != JDBCResultSet.FETCH_REVERSE && direction != JDBCResultSet.FETCH_UNKNOWN) {
    throw Util.notSupported();
  }
  fetchDirection=direction;
}
