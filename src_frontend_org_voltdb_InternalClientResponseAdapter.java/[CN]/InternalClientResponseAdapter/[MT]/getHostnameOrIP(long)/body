{
  InternalCallback callback=m_callbacks.get(clientHandle);
  if (callback == null) {
    m_logger.rateLimitedLog(ImportHandler.SUPPRESS_INTERVAL,Level.WARN,null,"Could not find caller details for client handle %d. Using internal adapter name",clientHandle);
    return getHostnameOrIP();
  }
 else {
    return callback.getInternalContext().getName();
  }
}
