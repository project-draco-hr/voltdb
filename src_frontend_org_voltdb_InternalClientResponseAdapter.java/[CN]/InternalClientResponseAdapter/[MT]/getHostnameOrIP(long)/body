{
  InternalCallback callback=m_callbacks.get(clientHandle);
  if (callback == null) {
    rateLimitedLog(Level.WARN,null,"Could not find caller details for client handle %d. Using internal adapter name",clientHandle);
    return getHostnameOrIP();
  }
 else {
    return callback.getInternalContext().getName();
  }
}
