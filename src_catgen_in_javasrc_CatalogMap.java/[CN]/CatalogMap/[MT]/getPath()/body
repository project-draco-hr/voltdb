{
  if (m_cachedPath != null) {
    return m_cachedPath;
  }
  String path=m_parent == m_catalog ? m_name : (m_parent.getCatalogPath() + "/" + m_name);
  if (m_depth <= 3) {
    m_cachedPath=path;
  }
  return path;
}
