{
  if (m_table != null) {
    return;
  }
  JoinNode[] children={m_leftNode,m_rightNode};
  for (  JoinNode child : children) {
    if (child.m_table != null) {
      continue;
    }
    if (child.m_joinType == m_joinType) {
      child.extractSubTree(leafNodes);
    }
 else {
      leafNodes.add(child);
      JoinNode tempNode=new JoinNode(-child.m_id,child.m_joinType,new Table(),child.m_joinExpr,child.m_whereExpr);
      if (child == m_leftNode) {
        m_leftNode=tempNode;
      }
 else {
        m_rightNode=tempNode;
      }
    }
  }
}
