{
  if (m_inputDepIds == null)   return -1;
  @SuppressWarnings("unchecked") ArrayList<Integer> l=(ArrayList<Integer>)m_inputDepIds[index];
  if (l == null)   return -1;
  assert(l.size() == 1);
  return l.get(0);
}
