{
  ProcProfRow in=new ProcProfRow(procedure,partition,invocations,timedInvocations,min,max,avg,failures,aborts);
  ProcProfRow exists=m_table.ceiling(in);
  if (exists != null && in.procedure.equals(exists.procedure)) {
    exists.updateWith(in);
  }
 else {
    m_table.add(in);
  }
}
