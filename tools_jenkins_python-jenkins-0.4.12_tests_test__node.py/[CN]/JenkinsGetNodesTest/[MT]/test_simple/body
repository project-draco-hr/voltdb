@patch.object(jenkins.Jenkins, 'jenkins_open')
def test_simple(self, jenkins_mock):
    jenkins_mock.return_value = json.dumps({'computer': [{'displayName': 'master', 'offline': False, }], 'busyExecutors': 2, })
    self.assertEqual(self.j.get_nodes(), [{'name': 'master', 'offline': False, }])
    self._check_requests(jenkins_mock.call_args_list)
