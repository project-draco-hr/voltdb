def __init__(self, name, function, java_class, **kwargs):
    CommandVerb.__init__(self, name, function, **kwargs)
    self.java_class = java_class
    self.add_cli_options(cli.IntegerOption('-R', '--remotedebug', 'remotedebug', 'enable remote debugging on specified port, e.g. 9090'))
