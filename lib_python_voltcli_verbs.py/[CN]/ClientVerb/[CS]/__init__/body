def __init__(self, name, function, **kwargs):
    utility.kwargs_merge_list(kwargs, 'cli_options', cli.CLIValue('-H', '--host', 'host', 'the target host as HOST[:PORT]', required=True))
    CommandVerb.__init__(self, name, function, **kwargs)
