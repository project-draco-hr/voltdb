{
  if (!m_initializedTableSaveFileNames.add(tableName)) {
    return;
  }
  TableSaveFile savefile=getTableSaveFile(getSaveFileForReplicatedTable(tableName),st.getLocalSites().length * 4,null);
  m_saveFiles.offer(savefile);
  assert(m_saveFiles.peekLast().getCompleted());
}
