{
  try {
    if (this.type == TYPE_EXEC)     return connection.callProcedure(this.sql[0],this.parameters).getResults();
 else     return connection.callProcedure("@AdHoc",this.sql[0]).getResults();
  }
 catch (  Exception x) {
    throw SQLError.get(x,SQLError.GENERAL_ERROR);
  }
}
