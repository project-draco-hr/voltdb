{
  try {
    if (this.type == TYPE_EXEC)     return connection.execute(this.sql[0],this.parameters).getResults();
 else     return connection.execute("@AdHoc",this.sql[0]).getResults();
  }
 catch (  Exception x) {
    throw SQLError.get(x,SQLError.GENERAL_ERROR);
  }
}
