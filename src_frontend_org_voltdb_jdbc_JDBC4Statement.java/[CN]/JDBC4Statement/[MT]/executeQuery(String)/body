{
  checkClosed();
  VoltSQL query=VoltSQL.parseSQL(sql);
  if (query.hasParameters() || !query.isOfType(VoltSQL.TYPE_SELECT))   throw SQLError.get(SQLError.ILLEGAL_STATEMENT,sql);
  return this.executeQuery(query);
}
