{
  super(mailbox,procName,new MpTransactionState(mailbox,msg,pInitiators,buddyHSId,isRestart),queue);
  m_isRestart=isRestart;
  m_msg=msg;
  m_initiatorHSIds.addAll(pInitiators);
  m_restartMasters.set(new ArrayList<Long>());
}
