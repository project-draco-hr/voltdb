{
  m_timeoutReaperHandle.cancel(false);
  m_ex.shutdown();
  m_ex.awaitTermination(1,TimeUnit.SECONDS);
  if (m_statsLoader != null) {
    m_statsLoader.stop();
  }
  m_network.shutdown();
synchronized (this) {
    m_pool.clear();
  }
}
