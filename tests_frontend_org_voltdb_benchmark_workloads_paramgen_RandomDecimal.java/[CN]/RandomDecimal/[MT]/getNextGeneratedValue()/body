{
  String temp="";
  if (Math.random() < 0.5)   temp+="-";
  for (int i=0; i < 26; i++)   temp+=(int)(Math.random() * 10);
  temp+=".";
  for (int i=0; i < 12; i++)   temp+=(int)(Math.random() * 10);
  return new BigDecimal(temp);
}
