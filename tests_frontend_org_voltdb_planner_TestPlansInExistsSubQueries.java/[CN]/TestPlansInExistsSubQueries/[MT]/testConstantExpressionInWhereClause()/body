{
  failToCompile("select * from r1 where 3 > 1;","VoltDB does not support WHERE clauses containing only constants");
  failToCompile("select a from r1 where exists " + " (select max(c) from r2) and 3 > 1;","VoltDB does not support WHERE clauses containing only constants");
}
