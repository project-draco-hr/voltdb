{
  timer=new Timer();
  TimerTask statsPrinting=new TimerTask(){
    @Override public void run(){
      sendStatistics();
    }
  }
;
  timer.scheduleAtFixedRate(statsPrinting,statsDuration * 1000,statsDuration * 1000);
}
