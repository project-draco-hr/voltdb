{
  if (tables != null) {
    stringer.key("tableIds");
    stringer.array();
    for (    Table table : tables) {
      stringer.value(table.getRelativeIndex());
    }
    stringer.endArray();
  }
}
