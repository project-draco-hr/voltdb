{
  ID=20;
  String raw=" insert into votes (phone-number, state, CONTESTANT_NUMBER) " + "values (978-475-      0001, 'MA', null);";
  String expected=raw.replaceAll("\\s*;+\\s*",";");
  assertThis(raw,expected,1,ID);
}
