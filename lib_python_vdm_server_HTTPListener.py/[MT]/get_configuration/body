def get_configuration():
    deployment_json = {'voltdeploy': {'databases': Global.DATABASES, 'members': Global.SERVERS, 'deployments': Global.DEPLOYMENT, 'deployment_users': Global.DEPLOYMENT_USERS, }, }
    return deployment_json
