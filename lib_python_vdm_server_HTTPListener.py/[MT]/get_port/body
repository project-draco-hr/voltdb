def get_port(ip_with_port):
    if (ip_with_port != ''):
        if (':' not in ip_with_port):
            return ip_with_port
        else:
            arr = ip_with_port.split(':')
            return arr[1]
