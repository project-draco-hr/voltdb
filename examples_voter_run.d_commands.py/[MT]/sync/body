@Command(description='Run the Voter synchronous benchmark. Use --help for usage.', passthrough=True)
def sync(runner):
    if (not os.path.exists('voter.jar')):
        runner.run('catalog')
    runner.java('voter.SyncBenchmark', None, *runner.args)
