@Command(description='Run the Voter asynchronous benchmark. Use --help for usage.', passthrough=True)
def async(runner):
    if (not os.path.exists('voter.jar')):
        runner.run('catalog')
    runner.java('voter.AsyncBenchmark', None, *runner.args)
