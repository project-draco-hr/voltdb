@Command(description='Run the Voter JDBC benchmark. Use --help for usage.', passthrough=True)
def jdbc(runner):
    if (not os.path.exists('voter.jar')):
        runner.run('catalog')
    runner.java('voter.JDBCBenchmark', None, *runner.args)
