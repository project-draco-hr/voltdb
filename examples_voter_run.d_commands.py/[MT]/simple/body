@Command(description='Run the Voter simple benchmark. Use --help for usage.', passthrough=True)
def simple(runner):
    if (not os.path.exists('voter.jar')):
        runner.run('catalog')
    runner.java('voter.SimpleBenchmark', None, 'localhost', *runner.args)
