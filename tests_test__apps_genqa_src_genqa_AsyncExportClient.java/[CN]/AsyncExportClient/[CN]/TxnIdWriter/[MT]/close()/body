{
  if (m_curFOS != null) {
    m_outs.close();
    m_curFOS.flush();
    m_curFOS.close();
    File renamed=new File(m_txnLogPath,m_curFile.getName().substring("active-".length()));
    m_curFile.renameTo(renamed);
  }
}
