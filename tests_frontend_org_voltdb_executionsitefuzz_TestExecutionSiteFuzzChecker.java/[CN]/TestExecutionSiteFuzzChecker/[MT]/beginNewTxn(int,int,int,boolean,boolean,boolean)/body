{
  StringWriter sw=m_siteLogs.get(partitionId).get(siteId);
  String msg="FUZZTEST beginNewTxn " + txnId + (isMulti ? " multi" : " single")+ " "+ (isReadOnly ? "readonly" : "readwrite")+ " "+ (isCoord ? "coord" : "part");
  sw.getBuffer().append(msg + "\n");
}
