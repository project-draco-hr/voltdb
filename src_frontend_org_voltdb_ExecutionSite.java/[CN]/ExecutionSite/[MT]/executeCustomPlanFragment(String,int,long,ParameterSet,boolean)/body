{
  VoltTable retval=null;
  try {
    ee.loadPlanFragment(-1,plan);
    retval=ee.executePlanFragment(-1,inputDepId,params,txnId,lastCommittedTxnId,readOnly ? Long.MAX_VALUE : getNextUndoToken());
  }
  finally {
    ee.unloadPlanFragment(-1);
  }
  return retval;
}
