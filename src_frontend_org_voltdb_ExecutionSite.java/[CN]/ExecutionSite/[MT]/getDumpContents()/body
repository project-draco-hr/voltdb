{
  final ExecutorContext context=new ExecutorContext();
  context.siteId=siteId;
  if (dtxnConn instanceof SimpleDtxnConnection)   ((SimpleDtxnConnection)dtxnConn).getDumpContents(context);
  return context;
}
