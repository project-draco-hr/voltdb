def DropWikiSuffix(wiki_filename):
    'Removes the .wiki suffix (if any) from the given filename.'
    return (wiki_filename[:(- len('.wiki'))] if wiki_filename.endswith('.wiki') else wiki_filename)
