{
  boolean found=false;
  while (!isDone() && !found) {
    if (!currentLog().isFuzz()) {
      m_logIndex++;
    }
 else     if (currentLog().isOtherFault()) {
      m_logIndex++;
    }
 else     if (currentLog().isSelfFault()) {
      m_logIndex++;
    }
 else {
      found=true;
    }
  }
}
