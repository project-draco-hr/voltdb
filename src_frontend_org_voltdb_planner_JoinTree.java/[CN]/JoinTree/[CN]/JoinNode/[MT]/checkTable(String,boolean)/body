{
  if (m_table != null) {
    return false;
  }
  assert(m_rightNode != null && m_leftNode != null);
  List<Table> innerTables=(checkRight == true) ? m_rightNode.generateTableJoinOrder() : m_leftNode.generateTableJoinOrder();
  for (  Table table : innerTables) {
    if (table.getTypeName().equals(tableName)) {
      return true;
    }
  }
  return false;
}
