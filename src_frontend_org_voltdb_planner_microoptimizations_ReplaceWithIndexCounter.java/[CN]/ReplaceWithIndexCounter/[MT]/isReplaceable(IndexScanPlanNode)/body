{
  AbstractExpression endExpr=child.getEndExpression();
  AbstractExpression postExpr=child.getPredicate();
  if (endExpr != null)   System.err.println("End Expression:\n" + endExpr);
  if (postExpr != null)   System.err.println("Post Expression:\n" + postExpr);
  return true;
}
