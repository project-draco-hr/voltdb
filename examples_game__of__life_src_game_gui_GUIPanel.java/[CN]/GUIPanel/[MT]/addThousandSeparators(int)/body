{
  String temp="" + num;
  String result="";
  for (int i=temp.length() - 1; i >= 0; i--) {
    result="" + temp.charAt(i) + result;
    if ((temp.length() - i) % 3 == 0 && i != 0)     result="," + result;
  }
  return result;
}
