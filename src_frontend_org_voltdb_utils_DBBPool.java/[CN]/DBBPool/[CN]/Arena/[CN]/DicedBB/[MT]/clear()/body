{
  if (m_availableSlices + m_returnedSlices != m_numSlices - 1) {
    throw new RuntimeException("Attempted to clear A DicedByteBuffer " + " while some portions were loaned out");
  }
  for (int ii=0; ii < m_slices.length; ii++) {
    m_slices[ii].b=null;
    m_slices[ii].address=0;
  }
}
