{
  final long estNow=EstTime.m_now.get();
  if (estNow == now) {
    return false;
  }
  EstTime.m_now.lazySet(now);
  if (now - estNow > 2000) {
    return true;
  }
 else {
    return false;
  }
}
