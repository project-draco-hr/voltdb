{
  ArrayList<Integer> userIds=new ArrayList<Integer>();
  String userHome=System.getProperty("user.home");
  String[] myOptions={"--inputfile=" + userHome + "/workspace/voltdb/doc/tutorials/auction/src/com/auctionexample/datafiles/users.txt","--procedurename=InsertIntoUser","--abortfailurecount=50"};
  new CSVLoader(myOptions);
  while (CSVLoader.readNext()) {
    userIds.add(Integer.parseInt(CSVLoader.insertLine(null,8)[0]));
  }
  CSVLoader.drain();
  CSVLoader.produceFiles();
  CSVLoader.flush();
  return userIds;
}
