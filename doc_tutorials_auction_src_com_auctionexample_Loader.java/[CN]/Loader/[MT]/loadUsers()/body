{
  ArrayList<Integer> userIds=new ArrayList<Integer>();
  URL url=Loader.class.getResource("datafiles/users.txt");
  String[] myOptions={"--file=" + url.getPath(),"--procedure=InsertIntoUser","--maxerrors=50","--user=program","--password=pass","--port="};
  try {
    CSVLoader.main(myOptions);
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
catch (  InterruptedException e) {
    e.printStackTrace();
  }
  ArrayList<String> list=CSVLoader.getFirstIds();
  for (  String id : list) {
    userIds.add(Integer.parseInt(id));
  }
  return userIds;
}
