{
  ArrayList<Integer> itemIds=new ArrayList<Integer>();
  URL url=Loader.class.getResource("datafiles/items.txt");
  String[] myOptions={"--file=" + url.getPath(),"--procedure=InsertIntoItem","--maxerrors=50","--user=program","--password=pass","--port=","--skipPreChecks=true"};
  try {
    CSVLoader.main(myOptions);
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
catch (  InterruptedException e) {
    e.printStackTrace();
  }
  ArrayList<Object> list=CSVLoader.getFirstIds();
  for (  Object id : list) {
    itemIds.add((Integer)id);
  }
  return itemIds;
}
