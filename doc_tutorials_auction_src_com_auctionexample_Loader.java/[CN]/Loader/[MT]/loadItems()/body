{
  ArrayList<Integer> itemIds=new ArrayList<Integer>();
  URL url=Loader.class.getResource("datafiles/items.txt");
  String[] myOptions={"--file=" + url.getPath(),"--procedure=InsertIntoItemAndBid","--maxerrors=50","--user=program","--password=pass","--port=","--skipChecks"};
  try {
    CSVLoader.main(myOptions);
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
catch (  InterruptedException e) {
    e.printStackTrace();
  }
  ArrayList<String> list=CSVLoader.getFirstIds();
  for (  String id : list) {
    itemIds.add(Integer.parseInt(id));
  }
  return itemIds;
}
