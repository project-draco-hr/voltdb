{
  AbstractPlanNode pn;
  List<AbstractPlanNode> planNodes;
  AbstractPlanNode nlpn;
  planNodes=compileToFragments("SELECT * from p1, p2 where p1.a = 1 and p2.a = 1");
  assertEquals(1,planNodes.size());
}
