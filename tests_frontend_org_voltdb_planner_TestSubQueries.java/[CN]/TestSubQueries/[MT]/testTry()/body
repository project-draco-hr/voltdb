{
  List<AbstractPlanNode> planNodes;
  planNodes=compileToFragments("select A1, A2, D1, D2 " + "FROM (SELECT A A1, D D1 FROM P1 WHERE A=2) T1, " + "(SELECT A A2, D D2 FROM P2 WHERE A=2) T2");
  for (  AbstractPlanNode apn : planNodes)   System.out.println(apn.toExplainPlanString());
}
