{
  this.ContestantCount=contestantCount;
  for (int i=0; i < VotingMap.length; i++)   VotingMap[i]=this.Rand.nextInt(100) < 30 ? 1 : (int)(Math.abs(Math.sin(i) * this.ContestantCount) % this.ContestantCount) + 1;
}
