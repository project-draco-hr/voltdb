{
  if (m.m_m.isPoll()) {
    ELTProtoMessage r=new ELTProtoMessage(m.m_m.getPartitionId(),m.m_m.getTableId());
    ByteBuffer data=ByteBuffer.allocate(8);
    data.putInt(100);
    data.putInt(200);
    r.pollResponse(2000,data);
    eequeue.add(r);
  }
}
