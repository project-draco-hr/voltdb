{
  boolean result=(m_data.size() == 0) && (!m_rowFailed);
  if (!result) {
    System.out.println("SingleTableVerifier error. Data size: " + m_data.size() + " row failed state: "+ m_rowFailed);
  }
  return result;
}
