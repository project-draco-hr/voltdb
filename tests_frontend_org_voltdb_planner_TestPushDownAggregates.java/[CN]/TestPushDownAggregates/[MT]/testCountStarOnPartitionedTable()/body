{
  List<AbstractPlanNode> pn=compile("SELECT count(*) from T1",0,false);
  checkPushedDown(pn,true,new ExpressionType[]{ExpressionType.AGGREGATE_COUNT_STAR},new ExpressionType[]{ExpressionType.AGGREGATE_SUM});
}
