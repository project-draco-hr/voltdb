{
  if (createPath) {
    m_zk.create("/hostids",null,Ids.OPEN_ACL_UNSAFE,CreateMode.PERSISTENT);
  }
  String node=m_zk.create("/hostids/host",address.getBytes("UTF-8"),Ids.OPEN_ACL_UNSAFE,CreateMode.PERSISTENT_SEQUENTIAL);
  return Integer.valueOf(node.substring(node.length() - 10));
}
