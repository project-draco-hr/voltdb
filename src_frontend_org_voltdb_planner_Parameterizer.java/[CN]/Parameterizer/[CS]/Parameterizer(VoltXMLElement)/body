{
  m_root=xmlSQL;
  assert(m_root != null);
  for (  VoltXMLElement child : m_root.children) {
    if (child.name.equals("parameters")) {
      m_paramsNode=child;
    }
  }
  assert(m_paramsNode != null);
}
