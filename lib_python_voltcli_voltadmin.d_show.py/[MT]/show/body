@VOLT.Multi_Command(bundles=VOLT.AdminBundle(), description='Display information about a live database.', modifiers=VOLT.Modifier('snapshots', show_snapshots, 'Display current snapshot status.'))
def show(runner):
    runner.go()
