{
  VoltTable results=new VoltTable(TABLE_SCHEMA);
  for (  Table table : m_database.getTables()) {
    results.addRow(m_jarFilename,null,table.getTypeName(),getTableType(table),null,null,null,null,null,null);
  }
  return results;
}
