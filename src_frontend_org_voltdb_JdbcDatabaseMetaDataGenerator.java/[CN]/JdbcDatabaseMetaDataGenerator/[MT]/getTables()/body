{
  VoltTable results=new VoltTable(TABLE_SCHEMA);
  for (  Table table : m_database.getTables()) {
    results.addRow(null,null,table.getTypeName(),getTableType(table),null,null,null,null,null,null);
  }
  return results;
}
