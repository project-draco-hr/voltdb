{
  VoltTable results=new VoltTable(PROCEDURES_SCHEMA);
  for (  Procedure proc : m_database.getProcedures()) {
    results.addRow(m_jarFilename,null,proc.getTypeName(),null,null,null,null,null,null);
  }
  return results;
}
