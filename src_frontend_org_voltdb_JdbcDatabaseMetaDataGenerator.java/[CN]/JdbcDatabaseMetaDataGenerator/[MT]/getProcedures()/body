{
  VoltTable results=new VoltTable(PROCEDURES_SCHEMA);
  for (  Procedure proc : m_database.getProcedures()) {
    results.addRow(null,null,proc.getTypeName(),null,null,null,null,java.sql.DatabaseMetaData.procedureResultUnknown,proc.getTypeName());
  }
  return results;
}
