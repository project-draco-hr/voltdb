{
  VoltTable results=new VoltTable(PROCEDURECOLUMNS_SCHEMA);
  for (  Procedure proc : m_database.getProcedures()) {
    for (    ProcParameter param : proc.getParameters()) {
      results.addRow(m_jarFilename,null,proc.getTypeName(),param.getTypeName(),java.sql.DatabaseMetaData.procedureColumnIn,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null);
    }
  }
  return results;
}
