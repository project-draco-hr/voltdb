{
  User sysUser=db.getUserManager().getSysUser();
  sysSession=new Session(db,sysUser,false,false,false,sessionIdCount++,0);
  sysLobSession=new Session(db,sysUser,true,true,false,sessionIdCount++,0);
}
