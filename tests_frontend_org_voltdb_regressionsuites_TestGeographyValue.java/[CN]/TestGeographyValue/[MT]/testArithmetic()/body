{
  Client client=getClient();
  fillTable(client,0);
  verifyStmtFails(client,"select pk, poly + poly from t order by pk","incompatible data type in conversion");
  verifyStmtFails(client,"select pk, poly + 1 from t order by pk","incompatible data type in conversion");
}
