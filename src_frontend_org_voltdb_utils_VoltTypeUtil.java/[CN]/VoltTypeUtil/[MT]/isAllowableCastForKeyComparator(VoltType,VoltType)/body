{
  if (from == to)   return true;
  if (from.isInteger()) {
    if (to.isInteger()) {
      return true;
    }
 else     if (to == VoltType.FLOAT) {
      if (from != VoltType.BIGINT) {
        return true;
      }
    }
  }
  return false;
}
