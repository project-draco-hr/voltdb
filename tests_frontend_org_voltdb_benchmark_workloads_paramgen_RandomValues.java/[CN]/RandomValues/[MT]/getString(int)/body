{
  String randomString="";
  double rand=Math.random();
  while ((double)randomString.length() / maxLength < rand) {
    randomString+=getChar();
    rand=Math.random();
  }
  return randomString;
}
