{
  for (int ii=0; ii < data.length; ++ii) {
    dut.updateTable(data[ii].timestamp,data[ii].procedure,data[ii].partition,data[ii].invocations,data[ii].min,data[ii].max,data[ii].avg,data[ii].failures,data[ii].aborts);
  }
}
