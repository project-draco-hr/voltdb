{
  String uuid=UUID.randomUUID().toString();
  Date now=new Date();
  long val=(long)(rand.nextGaussian() * 1000.0);
  try {
    insertsClient.callProcedure(new InsertCallback(),"TIMEDATA.insert",uuid,val,now);
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
}
