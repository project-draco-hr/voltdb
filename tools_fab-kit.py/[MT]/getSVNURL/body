def getSVNURL(defaultPrefix, input):
    input = input.strip()
    if input.startswith('http'):
        return input
    if (input[0] == '/'):
        input = input[1:]
    return (defaultPrefix + input)
