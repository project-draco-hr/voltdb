{
  for (  ZooKeeper keeper : m_clients) {
    keeper.close();
  }
  m_clients.clear();
  for (  AgreementSite site : m_agreementSites) {
    if (site != null) {
      site.shutdown();
    }
  }
  m_agreementSites.clear();
}
