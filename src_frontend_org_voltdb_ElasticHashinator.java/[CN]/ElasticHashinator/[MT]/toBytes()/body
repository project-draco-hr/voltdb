{
  ByteBuffer buf=ByteBuffer.allocate(4 + (tokens.size() * 12));
  buf.putInt(tokens.size());
  for (  Map.Entry<Long,Integer> e : tokens.entrySet()) {
    long token=e.getKey();
    int pid=e.getValue();
    buf.putLong(token);
    buf.putInt(pid);
  }
  return buf.array();
}
