{
  compile("insert into target_p " + "select * from source_p1");
  compile("insert into target_p (bi, vc, ii, ti) select * from source_p1");
  compile("insert into target_p " + "select sp1.bi, sp1.vc, sp2.ii, sp2.ti " + "from source_p1 as sp1 inner join source_p2 as sp2 on sp1.bi = sp2.bi "+ "where sp1.bi between ? and ?;");
}
