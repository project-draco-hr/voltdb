{
  VoltDB.Configuration cfg=new VoltDB.Configuration(new String[]{"enableiv2","leader","localhost","deployment","d.xml","catalog","catalog.jar"});
  assertTrue(cfg.validate());
  MockVoltDB vdb=new MockVoltDB(null,null);
  vdb.initialize(cfg);
  assertTrue(vdb.isIV2Enabled());
}
