{
  for (int i=0; i < m_iterations; i++)   try {
    long start=System.nanoTime();
    String jsonResponse=TestJSONInterface.callProcOverJSON("Select",pset,null,null);
    long stop=System.nanoTime();
    totalExecTime+=stop - start;
  }
 catch (  Exception e) {
    e.printStackTrace();
    System.exit(-1);
  }
}
