{
  VoltCacheProcBase.Result results=null;
  try {
    if (noreply) {
      results=this.client.callProcedure(VoltCache.nullCallback,procedure,parameters) ? VoltCacheProcBase.Result.SUBMITTED() : VoltCacheProcBase.Result.ERROR();
    }
 else {
      results=getResult(type,this.client.callProcedure(procedure,parameters));
    }
  }
 catch (  Exception x) {
    results=VoltCacheProcBase.Result.ERROR();
  }
  return results;
}
