{
  List<AbstractPlanNode> pn=compile("SELECT count(*) from T2 WHERE POINTS > ? AND NAME ='XIN' ",1,true);
  checkIndexCounter(pn,false,new ExpressionType[]{ExpressionType.AGGREGATE_COUNT_STAR},null);
}
