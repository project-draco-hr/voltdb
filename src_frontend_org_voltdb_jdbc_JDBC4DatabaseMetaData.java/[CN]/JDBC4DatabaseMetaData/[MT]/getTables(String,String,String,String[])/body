{
  if ((tableNamePattern != null && !tableNamePattern.equals("%")) || types != null)   throw new SQLException(String.format("getTables('%s','%s','%s',%d) does not support pattern filtering",catalog,schemaPattern,tableNamePattern,types != null ? types.length : 0));
  checkClosed();
  this.sysCatalog.setString(1,"TABLES");
  return this.sysCatalog.executeQuery();
}
