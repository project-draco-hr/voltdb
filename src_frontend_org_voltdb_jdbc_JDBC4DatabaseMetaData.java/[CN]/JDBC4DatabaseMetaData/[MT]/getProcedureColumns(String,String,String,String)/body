{
  if (procedureNamePattern != null || columnNamePattern != null)   throw new SQLException("getProcedureColumns() does not support pattern filtering");
  checkClosed();
  this.sysCatalog.setString(1,"PROCEDURECOLUMNS");
  ResultSet res=this.sysCatalog.executeQuery();
  return res;
}
