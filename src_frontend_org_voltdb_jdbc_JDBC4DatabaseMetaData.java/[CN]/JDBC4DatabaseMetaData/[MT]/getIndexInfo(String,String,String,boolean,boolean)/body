{
  if (table != null)   throw new SQLException("getIndexInfo() does not support pattern filtering");
  checkClosed();
  this.sysCatalog.setString(1,"INDEXINFO");
  ResultSet res=this.sysCatalog.executeQuery();
  return res;
}
