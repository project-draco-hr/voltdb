{
  if (tableNamePattern != null || columnNamePattern != null)   throw new SQLException("getColumns() does not support pattern filtering");
  checkClosed();
  this.sysCatalog.setString(1,"COLUMNS");
  ResultSet res=this.sysCatalog.executeQuery();
  return res;
}
