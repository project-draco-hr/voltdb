{
  statementErrorTest("INTERSECT","(select ival from blah) intersect (select ival from indexed_blah);");
  statementErrorTest("INTERSECT","select ival from blah intersect select ival from indexed_blah;");
}
