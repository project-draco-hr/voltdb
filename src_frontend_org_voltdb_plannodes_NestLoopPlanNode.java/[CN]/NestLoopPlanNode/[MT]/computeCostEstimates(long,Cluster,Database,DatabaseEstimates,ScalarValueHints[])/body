{
  m_estimatedOutputTupleCount=childOutputTupleCountEstimate;
  assert(m_children.size() == 2);
  m_estimatedProcessedTupleCount=discountEstimatedProcessedTupleCount(m_children.get(0)) + m_children.get(1).m_estimatedProcessedTupleCount;
}
