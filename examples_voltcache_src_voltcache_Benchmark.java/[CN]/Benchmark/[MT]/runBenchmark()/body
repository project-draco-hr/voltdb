{
  connect();
  final PayloadProcessor processor=initPayload();
  Timer timer=startStatsTimer();
  try {
    executePayload(processor);
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
  timer.cancel();
  displayStatistics();
  this.cache.close();
}
