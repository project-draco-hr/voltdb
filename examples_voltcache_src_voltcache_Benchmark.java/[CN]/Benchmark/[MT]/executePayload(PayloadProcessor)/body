{
  ArrayList<Thread> threads=new ArrayList<Thread>();
  for (int i=0; i < this.config.threads; i++)   threads.add(new Thread(new ClientThread(processor,this.config.duration,this.config.getputratio)));
  for (  Thread thread : threads)   thread.start();
  for (  Thread thread : threads)   thread.join();
}
