{
  int lastGoodChar=buf.length;
  for (int i=0; i < buf.length; i++) {
    int bint=new Byte(buf[i]).intValue();
    if (bint == 0) {
      lastGoodChar=i;
      break;
    }
  }
  return lastGoodChar;
}
