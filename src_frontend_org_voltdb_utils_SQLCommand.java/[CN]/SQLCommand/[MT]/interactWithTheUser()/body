{
  List<QueryInfo> queryBatchList=null;
  while (true) {
    try {
      queryBatchList=getInteractiveQueries();
    }
 catch (    Exception ex) {
      stopOrContinue(ex);
      continue;
    }
    if (queryBatchList == null)     break;
    executeQueryWithBatches(queryBatchList);
  }
}
