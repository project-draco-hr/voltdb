def normalize(table, sql):
    'Normalizes the result tuples of ORDER BY statements, sorting SQL NULL\n       (Python None) values as if they were the highest values, i.e., last\n       when using ORDER BY col1 ASC, but first when using ORDER BY col1 DESC.\n    '
    return StandardNormalizer.normalize(table, sql, SortNulls.highest)
