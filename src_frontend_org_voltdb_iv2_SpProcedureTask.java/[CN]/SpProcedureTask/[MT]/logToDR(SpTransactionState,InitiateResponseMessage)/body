{
  if (m_drGateway != null && !txn.isReadOnly() && !txn.needsRollback()) {
    m_drGateway.onSuccessfulProcedureCall(txn.txnId,txn.uniqueId,txn.getHash(),txn.getInvocation(),response.getClientResponseData());
  }
}
