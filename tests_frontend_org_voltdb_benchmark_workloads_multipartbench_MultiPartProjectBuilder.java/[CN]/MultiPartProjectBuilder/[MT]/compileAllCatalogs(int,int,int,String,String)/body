{
  addAllDefaults();
  boolean compile=compile(m_jarFileName,sitesPerHost,length,kFactor,leader,voltRoot);
  if (!compile) {
    throw new RuntimeException("Multipart benchmark project builder failed app compilation.");
  }
  return new String[]{m_jarFileName};
}
