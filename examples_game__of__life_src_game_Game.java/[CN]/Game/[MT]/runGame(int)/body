{
  populateRandomly(initPopul);
  boolean repeat=true;
  while (repeat) {
    evolve();
    try {
      Thread.sleep(DELAY_MILLIS);
    }
 catch (    InterruptedException e) {
      e.printStackTrace();
    }
  }
  endGame();
}
