{
  long startTime;
  long endTime;
  if (DEBUG)   startTime=System.nanoTime();
  boolean futureBool;
  for (int i=0; i < NUM_CELLS; i++) {
    futureBool=determineFuture(i);
    future[i]=futureBool;
    db.occupy(futureBool,i);
  }
  for (int i=0; i < NUM_CELLS; i++)   ids[i]=future[i];
  db.updateGeneration();
  if (DEBUG) {
    endTime=System.nanoTime();
    System.out.println("Generation took " + ((double)(endTime - startTime) / 1000000000) + " seconds to calculate.");
    print();
  }
}
