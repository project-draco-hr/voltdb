@VOLT.Command(bundles=VOLT.ServerBundle('add', needs_catalog=False, supports_live=False, default_host=False, safemode_available=False, supports_daemon=True, supports_multiple_daemons=True, check_environment_config=True), description='Add the current node to a VoltDB cluster.')
def add(runner):
    runner.go()
