{
  NullProcedureWrapper wrapper=null;
  try {
    wrapper=procedure.newInstance();
  }
 catch (  InstantiationException e) {
    e.printStackTrace();
  }
catch (  IllegalAccessException e) {
    e.printStackTrace();
  }
  wrapper.init(site.getSiteTracker().m_numberOfPartitions,site,site.m_context.database.getProcedures().get(procedure.getName()),BackendTarget.NATIVE_EE_JNI,null,null);
  return wrapper.call(null,(Object)null);
}
