{
  NullProcedureWrapper wrapper=null;
  try {
    wrapper=procedure.newInstance();
  }
 catch (  InstantiationException e) {
    e.printStackTrace();
  }
catch (  IllegalAccessException e) {
    e.printStackTrace();
  }
  ProcedureRunner runner=new ProcedureRunner(wrapper,site,null,site.m_context.database.getProcedures().get(procedure.getName()),null);
  runner.setupTransaction(null);
  return runner.call((Object)null);
}
