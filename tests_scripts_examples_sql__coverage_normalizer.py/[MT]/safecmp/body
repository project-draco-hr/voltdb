def safecmp(x, y):
    for (xn, yn) in zip(x, y):
        if (xn is None):
            if (yn is None):
                continue
            return (-1)
        if (yn is None):
            return 1
        rn = cmp(xn, yn)
        if rn:
            return rn
    return cmp(len(x), len(y))
