{
  if (m_callingThreadsToBlock.get(Thread.currentThread().getId())) {
    return null;
  }
  return super.loadTable(tableId,table,txnId,spHandle,lastCommittedSpHandle,uniqueId,returnUniqueViolations,shouldDRStream,undoToken);
}
