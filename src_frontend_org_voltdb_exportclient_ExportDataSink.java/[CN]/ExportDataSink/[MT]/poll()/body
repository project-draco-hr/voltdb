{
  m_logger.trace("Polling table " + m_tableName + ", partition "+ m_partitionId+ " for new data.");
  ELTProtoMessage m=new ELTProtoMessage(m_partitionId,m_tableId);
  m.poll();
  m_txQueues.get(m_activeConnection).offer(m);
}
