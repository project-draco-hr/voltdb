{
  String literalSchema="CREATE TABLE places (\n" + "  pk         INTEGER NOT NULL PRIMARY KEY,\n" + "  name       VARCHAR(64),\n"+ "  loc        GEOGRAPHY_POINT\n"+ ");\n"+ "CREATE TABLE borders (\n"+ "  pk         INTEGER NOT NULL PRIMARY KEY,\n"+ "  name       VARCHAR(64),\n"+ "  message    VARCHAR(64),\n"+ "  region     GEOGRAPHY\n"+ ");\n"+ "\n";
  project.addLiteralSchema(literalSchema);
}
