{
  StringBuilder sb=new StringBuilder();
  String prefix="Parameter types: ";
  for (  byte typeByte : statementParamTypes) {
    sb.append(prefix).append(VoltType.get(typeByte).toSQLString());
    prefix=", ";
  }
  return sb.toString();
}
