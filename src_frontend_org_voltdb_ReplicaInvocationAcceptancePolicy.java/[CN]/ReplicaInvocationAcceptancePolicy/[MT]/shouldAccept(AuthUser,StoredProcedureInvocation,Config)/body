{
  if (invocation.getType() == ProcedureInvocationType.ORIGINAL && sysProc.allowedInReplica && !invocation.procName.equalsIgnoreCase("@AdHoc")) {
    return null;
  }
  return shouldAcceptHelper(user,invocation,sysProc.readOnly);
}
