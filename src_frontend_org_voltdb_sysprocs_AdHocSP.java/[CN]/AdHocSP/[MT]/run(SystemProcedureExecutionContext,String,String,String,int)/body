{
  assert(collectorFragment == null);
  ExecutionEngine ee=ctx.getExecutionEngine();
  VoltTable t;
  t=ee.executeCustomPlanFragment(aggregatorFragment,1,-1,getTransactionId(),ctx.getLastCommittedTxnId(),ctx.getNextUndo());
  return t;
}
