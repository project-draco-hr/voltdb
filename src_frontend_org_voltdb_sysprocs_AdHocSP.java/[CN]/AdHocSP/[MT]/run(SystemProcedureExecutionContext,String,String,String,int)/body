{
  assert(collectorFragment == null);
  SiteProcedureConnection spc=ctx.getSiteProcedureConnection();
  VoltTable t;
  t=spc.executeCustomPlanFragment(aggregatorFragment,-1,getTransactionId());
  return t;
}
