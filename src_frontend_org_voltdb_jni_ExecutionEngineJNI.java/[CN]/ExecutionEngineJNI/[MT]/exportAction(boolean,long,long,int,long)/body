{
  deserializer.clear();
  ExportProtoMessage result=null;
  long retval=nativeExportAction(pointer,syncAction,ackTxnId,seqNo,tableId);
  if (retval < 0) {
    result=new ExportProtoMessage(partitionId,tableId);
    result.error();
  }
  return result;
}
