{
  deserializer.clear();
  ExportProtoMessage result=null;
  long retval=nativeExportAction(pointer,syncAction,ackTxnId,seqNo,tableSignature);
  if (retval < 0) {
    result=new ExportProtoMessage(partitionId,tableSignature);
    result.error();
  }
  return result;
}
