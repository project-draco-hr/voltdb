{
  boolean result=(m_data.size() == 0) && (!m_rowFailed);
  if (!result) {
    System.out.println("ExportVerifier error. Table ID: " + m_tableName + ", partition ID: "+ m_partitionId);
    System.out.println("  Data size: " + m_data.size() + " row failed state: "+ m_rowFailed);
  }
  return result;
}
