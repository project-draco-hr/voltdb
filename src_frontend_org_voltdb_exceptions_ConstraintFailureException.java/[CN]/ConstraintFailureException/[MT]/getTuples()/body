{
  if (table != null) {
    return table;
  }
  table=new VoltTable();
  try {
    table.readExternal(new FastDeserializer(buffer));
  }
 catch (  IOException e) {
    LOG.severe(e.toString());
    return null;
  }
  return table;
}
