{
  if ("union".equalsIgnoreCase(childExpr.name) && unionExpr.attributes.get("uniontype").equalsIgnoreCase(childExpr.attributes.get("uniontype"))) {
    unionExpr.children.addAll(childExpr.children);
  }
 else {
    unionExpr.children.add(childExpr);
  }
  return unionExpr;
}
