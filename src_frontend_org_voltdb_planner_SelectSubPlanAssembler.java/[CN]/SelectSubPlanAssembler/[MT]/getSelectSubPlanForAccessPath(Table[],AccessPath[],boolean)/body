{
  AbstractPlanNode retv=getSelectSubPlanForAccessPathRecursive(joinOrder,accessPath,suppressSendReceivePair);
  if (isStatementMultiPartition() && m_countOfPartitionedTables > 0 && suppressSendReceivePair)   retv=addSendReceivePair(retv);
  return retv;
}
