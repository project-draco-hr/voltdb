{
  try {
    Thread.sleep(1000 * failedCount++);
    if (failedCount > 10)     failedCount=1;
  }
 catch (  InterruptedException e) {
    rateLimitedLog(Level.ERROR,e,"Interrupted sleep when checkpointing.");
  }
  return failedCount;
}
