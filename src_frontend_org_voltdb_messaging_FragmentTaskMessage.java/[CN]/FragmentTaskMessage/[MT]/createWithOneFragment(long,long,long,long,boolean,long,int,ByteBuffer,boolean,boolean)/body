{
  FragmentTaskMessage ret=new FragmentTaskMessage(initiatorHSId,coordinatorHSId,txnId,timestamp,isReadOnly,isFinal,isForReplay);
  ret.addFragment(fragmentId,outputDepId,parameterSet);
  return ret;
}
