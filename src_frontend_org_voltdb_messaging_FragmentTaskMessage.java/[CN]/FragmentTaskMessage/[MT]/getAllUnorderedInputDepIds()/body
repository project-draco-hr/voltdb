{
  int[] retval=new int[m_inputDepCount];
  int i=0;
  if (m_inputDepIds != null) {
    for (    ArrayList<?> l : m_inputDepIds) {
      @SuppressWarnings("unchecked") ArrayList<Integer> l2=(ArrayList<Integer>)l;
      for (      int depId : l2) {
        retval[i++]=depId;
      }
    }
  }
  assert(i == m_inputDepCount);
  return retval;
}
