{
  final String literalSchema="CREATE TABLE S1 ( " + "ID INTEGER DEFAULT 0 NOT NULL, " + "WAGE INTEGER, "+ "DEPT INTEGER, "+ "PRIMARY KEY (ID) );"+ "CREATE TABLE S2 ( "+ "ID INTEGER DEFAULT 0 NOT NULL, "+ "WAGE INTEGER, "+ "DEPT INTEGER, "+ "PRIMARY KEY (ID) );"+ "CREATE TABLE R1 ( "+ "ID INTEGER DEFAULT 0 NOT NULL, "+ "DESC VARCHAR(300), "+ "NUM INTEGER, "+ "RATIO FLOAT, "+ "PAST TIMESTAMP, "+ "PRIMARY KEY (ID) ); "+ "CREATE UNIQUE INDEX R1_ABS_ID_DESC ON R1 ( ABS(ID), DESC ); "+ "CREATE INDEX R1_ABS_ID_SCALED ON R1 ( ID / 3 ); "+ "CREATE INDEX R1_CASEWHEN ON R1 (CASE WHEN num < 3 THEN num/2 ELSE num + 10 END); "+ "CREATE TABLE INLINED_VC_VB_TABLE ("+ "ID INTEGER DEFAULT 0 NOT NULL,"+ "VC1 VARCHAR(6),"+ "VC2 VARCHAR(16),"+ "VB1 VARBINARY(6),"+ "VB2 VARBINARY(64));"+ "";
  project.addLiteralSchema(literalSchema);
}
