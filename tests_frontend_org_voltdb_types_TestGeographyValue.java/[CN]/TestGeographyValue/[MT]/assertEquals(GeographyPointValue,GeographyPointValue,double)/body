{
  String message=String.format("Expected: %s, actual %s",expected,actual);
  assertTrue(message,Math.abs(expected.getLongitude() - actual.getLongitude()) < epsilon && Math.abs(expected.getLatitude() - actual.getLatitude()) < epsilon);
}
