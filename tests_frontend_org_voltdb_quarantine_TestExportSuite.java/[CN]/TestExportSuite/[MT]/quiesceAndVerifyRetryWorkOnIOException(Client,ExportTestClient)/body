{
  quiesce(client);
  while (true) {
    try {
      tester.work(30000);
    }
 catch (    ExportClientException e) {
      e.printStackTrace();
      boolean success=reconnect(tester);
      assertTrue(success);
      continue;
    }
    break;
  }
  assertTrue(tester.allRowsVerified());
}
