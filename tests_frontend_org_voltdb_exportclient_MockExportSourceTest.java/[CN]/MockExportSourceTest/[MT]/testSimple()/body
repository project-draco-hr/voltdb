{
  new Thread(){
    @Override public void run(){
      MockExportSource.run(1,1,0);
    }
  }
.start();
  File dir=new File(System.getProperty("user.dir") + File.separator + "00_exportout");
  System.out.printf("Working dir is %s\n",dir.getPath());
  ExportToFileClient exportClient=new ExportToFileClient(',',"testy",dir,1,"yyyyMMddHHmmss","&amp;\u0000\\\n",0,false,true,true,5,false,TimeZone.getDefault(),ExportToFileClient.BinaryEncoding.HEX);
  exportClient.addServerInfo("localhost",false);
  exportClient.addCredentials("","");
  exportClient.run();
}
