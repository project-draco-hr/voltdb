def select(self):
    "\n        Generate the SQL to select this database\n          >>> schema.databases['sakila'].select()\n          'USE `sakila`;'\n        "
    return ('USE `%s`;' % self.name)
