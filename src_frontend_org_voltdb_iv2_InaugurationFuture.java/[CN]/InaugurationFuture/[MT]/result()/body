{
  if (m_exception != null) {
    throw m_exception;
  }
 else   if (isCancelled()) {
    return new Pair<Boolean,Long>(false,Long.MIN_VALUE);
  }
  return new Pair<Boolean,Long>(true,m_maxTxnId);
}
