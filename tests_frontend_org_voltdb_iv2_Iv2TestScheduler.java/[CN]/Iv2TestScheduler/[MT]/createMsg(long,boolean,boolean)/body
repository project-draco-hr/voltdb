{
  StoredProcedureInvocation spi=mock(StoredProcedureInvocation.class);
  when(spi.getProcName()).thenReturn(MockSPName);
  Iv2InitiateTaskMessage task=new Iv2InitiateTaskMessage(Long.MIN_VALUE,Long.MIN_VALUE,txnId,readOnly,singlePart,spi,Long.MAX_VALUE);
  return task;
}
