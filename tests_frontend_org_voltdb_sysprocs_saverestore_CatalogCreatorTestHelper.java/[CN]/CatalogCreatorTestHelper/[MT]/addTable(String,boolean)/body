{
  m_catalog.execute("add " + getDatabase().getPath() + " tables "+ tableName);
  String is_replicated="false";
  if (isReplicated) {
    is_replicated="true";
  }
  m_catalog.execute("set " + getTable(tableName).getPath() + " isreplicated "+ is_replicated);
  return m_catalog;
}
