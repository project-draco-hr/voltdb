{
  if (truncateTable1 != 0) {
    voltQueueSQL(renewbase1);
  }
  if (truncateTable2 != 0) {
    voltQueueSQL(renewbase2);
  }
  if (truncateTable3 != 0) {
    voltQueueSQL(renewbase3);
  }
  if (truncateTable4 != 0) {
    voltQueueSQL(renewbase4);
  }
  voltExecuteSQL();
}
