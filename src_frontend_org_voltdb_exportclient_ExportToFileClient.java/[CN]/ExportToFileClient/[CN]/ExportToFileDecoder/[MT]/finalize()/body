{
  if (m_Writer != null) {
    try {
      m_Writer.flush();
      m_Writer.close();
    }
 catch (    Exception e) {
      String msg=e.getMessage();
      if (msg.compareToIgnoreCase("stream closed") == 0)       m_logger.error(e.getMessage());
    }
  }
  super.finalize();
}
