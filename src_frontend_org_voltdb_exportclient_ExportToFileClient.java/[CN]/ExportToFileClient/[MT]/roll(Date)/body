{
synchronized (m_batchLock) {
    m_logger.trace("Rolling batch.");
    m_current.end=rollTime;
    if (m_current.m_decoders.size() == 0)     m_current.closeAllWriters();
    m_current=new PeriodicExportContext(rollTime);
  }
}
