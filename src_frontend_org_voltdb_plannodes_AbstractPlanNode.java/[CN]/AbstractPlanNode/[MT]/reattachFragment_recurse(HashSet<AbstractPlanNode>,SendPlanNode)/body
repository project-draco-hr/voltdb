{
  visited.add(this);
  for (  AbstractPlanNode pn : m_inlineNodes.values()) {
    if (pn.reattachFragment_recurse(visited,child))     return true;
  }
  for (  AbstractPlanNode pn : m_children) {
    if (pn.reattachFragment_recurse(visited,child))     return true;
  }
  return false;
}
