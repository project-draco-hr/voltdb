{
  if (hasSubquery()) {
    if (this.hasAnyNodeOfType(PlanNodeType.AGGREGATE) && this.hasAnyNodeOfType(PlanNodeType.HASHAGGREGATE) && this.hasAnyNodeOfType(PlanNodeType.LIMIT)) {
      return true;
    }
  }
  return false;
}
