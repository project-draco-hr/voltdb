def __init__(self, host, port, username='', password='', dump_file=None):
    cmd.Cmd.__init__(self)
    self.__quiet = False
    self.__timeout = None
    self.__initialize(host, port, username, password, dump_file)
