{
  return new CommitLog(){
    @Override public void logMessage(    VoltMessage message,    SiteMailbox mailbox){
      message.setDurable();
      mailbox.deliver(message);
    }
    @Override public void shutdown() throws InterruptedException {
    }
  }
;
}
