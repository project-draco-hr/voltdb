{
  return new CommandLog(){
    @Override public void shutdown() throws InterruptedException {
    }
    @Override public void init(    CatalogContext context){
    }
    @Override public void log(    InitiateTaskMessage message){
    }
    @Override public Semaphore logFault(    Set<Integer> failedSites,    Set<Long> faultedTxns){
      return new Semaphore(1);
    }
    @Override public void logHeartbeat(    long txnId){
    }
    @Override public void setFaultSequenceNumber(    long sequenceNumber,    Set<Integer> failedSites){
    }
  }
;
}
