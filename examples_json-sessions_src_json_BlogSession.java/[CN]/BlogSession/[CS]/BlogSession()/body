{
  super("VoltDB Blog");
  int randInt=random.nextInt(5000);
  if (randInt == 0) {
    roles=role_lists[2];
  }
 else   if (randInt < 50) {
    roles=role_lists[1];
  }
 else {
    roles=role_lists[0];
  }
}
