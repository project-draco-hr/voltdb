{
  if (myThread == null)   return;
  try {
    if (myThread.isAlive()) {
      if (myServerSocket != null)       myServerSocket.close();
      if (blocking)       myThread.join();
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
}
