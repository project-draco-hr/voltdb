{
  assert(request != null);
  assert(response != null);
  Work w=new Work();
  w.socket=request.socket;
  w.response=response;
  try {
    workQueue.put(w);
  }
 catch (  InterruptedException e) {
    e.printStackTrace();
  }
}
