{
  String prefix=URL_PREFIX + "//";
  int end=url.length();
  if (url.indexOf("?") > 0) {
    end=url.indexOf("?");
  }
  String servstring=url.substring(prefix.length(),end);
  return servstring.split(",");
}
