{
  long[] non_local=new long[count - 1];
  int index=0;
  for (long i=0; i < count; i++) {
    allHsids.add(i);
    if (i != localHsid) {
      non_local[index]=i;
      ++index;
    }
  }
  System.out.println(allHsids);
  return non_local;
}
