import sys
from optparse import OptionParser
import re
import subprocess
java_template = 'package {%package};\n\nimport org.voltdb.*;\n\npublic class {%classname} extends VoltProcedure {\n    public final SQLStmt stmt = new SQLStmt("{%statement}");\n\n    public VoltTable[] run({%type_para}) {\n        voltQueueSQL(stmt{%parameters});\n        return voltExecuteSQL();\n    }\n}'
volt_type = [('INVALID', ''), ('NULL', ''), ('NUMERIC', ''), ('TINYINT', 'byte'), ('SMALLINT', 'short'), ('INTEGER', 'int'), ('BIGINT', 'long'), ('NOTUSED', 'not used'), ('FLOAT', 'double'), ('STRING', 'String'), ('NOTUSED', 'not used'), ('TIMESTAMP', 'org.voltdb.types.TimestampType'), ('NOTUSED', 'not used'), ('NOTUSED', 'not used'), ('NOTUSED', 'not used'), ('NOTUSED', 'not used'), ('NOTUSED', 'not used'), ('NOTUSED', 'not used'), ('NOTUSED', 'not used'), ('NOTUSED', 'not used'), ('NOTUSED', 'not used'), ('NOTUSED', 'not used'), ('VOLTTABLE', 'VoltTable'), ('DECIMAL', 'java.math.BigDecimal'), ('NOTUSED', 'not used'), ('NOTUSED', 'not used'), ('VARBINARY', 'byte[]')]
if (__name__ == '__main__'):
    main()
