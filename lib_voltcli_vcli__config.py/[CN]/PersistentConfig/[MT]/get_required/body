def get_required(self, section, option):
    if (not self.loaded):
        self.load()
    value = self.get(section, option)
    if (value is None):
        self.abort(('Configuration parameter "%s.%s" was not found.' % (section, option)), 'Set parameters using the "config" command.')
    return value
