{
  if (data == null || data.getRowCount() == 0) {
    return;
  }
  try {
    m_site.loadTable(m_currentTxnState.txnId,clusterName,databaseName,tableName,data);
  }
 catch (  EEException e) {
    throw new VoltAbortException("Failed to load table: " + tableName);
  }
}
