{
  final long txnId=m_idManager.getNextUniqueTransactionId();
  final long now=m_idManager.getLastUsedTime();
  if (m_sendHeartbeats) {
    sendHeartbeat(txnId);
  }
  m_lastTickTime=now;
  return now;
}
