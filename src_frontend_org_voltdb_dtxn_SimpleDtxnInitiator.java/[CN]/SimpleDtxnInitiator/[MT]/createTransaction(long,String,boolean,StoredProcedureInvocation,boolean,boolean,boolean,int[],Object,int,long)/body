{
  long txnId;
  txnId=m_idManager.getNextUniqueTransactionId();
  boolean retval=createTransaction(connectionId,connectionHostname,adminConnection,txnId,txnId,invocation,isReadOnly,isSinglePartition,isEveryPartition,partitions,clientData,messageSize,now);
  return retval;
}
