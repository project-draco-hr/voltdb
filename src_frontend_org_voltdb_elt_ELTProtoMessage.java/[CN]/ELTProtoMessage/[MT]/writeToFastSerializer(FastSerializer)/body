{
  fs.writeInt(serializableBytes());
  fs.writeInt(m_type);
  fs.writeInt(m_partitionId);
  fs.writeInt(m_tableId);
  fs.writeLong(m_offset);
  if (m_data != null) {
    fs.write(m_data);
    m_data.flip();
  }
}
