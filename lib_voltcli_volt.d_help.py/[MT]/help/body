@Command(description='Display command help', usage='[COMMAND ...]')
def help(runner):
    if runner.args:
        for arg in runner.args:
            runner.help(arg)
    else:
        runner.usage()
